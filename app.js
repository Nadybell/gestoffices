const SEED = {"chants": [{"titre": "Que ma bouche chante ta louange", "reference1": null, "reference2": null, "texte": "1. De toi, Seigneur, nous attendons la vie,Que ma bouche chante ta louange !Tu es pour nous un rempart, un appui,Que ma bouche chante ta louange !La joie du cœur vient de toi, ô Seigneur,Que ma bouche chante ta louange !Notre confiance est dans ton nom très saint !Que ma bouche chante ta louange !R. Sois loué, Seigneur, pour ta grandeur,Sois loué pour tous tes bienfaits.Gloire à toi, Seigneur, tu es vainqueur,Ton amour inonde nos cœurs.Que ma bouche chante ta louange !2. Seigneur, tu as éclairé notre nuit,Que ma bouche…Tu es lumière et clarté sur nos pas,Que ma bouche…Tu affermis nos mains pour le combat,Que ma bouche…Seigneur, tu nous fortifies dans la foi !Que ma bouche…3. Tu viens sauver tes enfants égarés,Que ma bouche…Qui dans leur cœur espèrent en ton amour,Que ma bouche…Dans leur angoisse, ils ont crié vers toi,Que ma bouche…Seigneur, tu entends le son de leur voix !Que ma bouche…4. Des ennemis, toi, tu m’as délivré,Que ma bouche…De l’agresseur, tu m’as fait triompher,Que ma bouche…Je te rends grâce au milieu des nations,Que ma bouche…Seigneur, en tout temps, je fête ton Nom !Que ma bouche…", "type_messe": "ORD", "type_chant": "ENT", "compositeur": null, "harmonisation": null, "partition": null, "youtube": "https://youtu.be/KTXkDEen3m0?si=urAQpRRngYl1xrQA", "audios": []}, {"titre": "Goûtez et voyez", "reference1": null, "reference2": "IEV 21-06", "texte": "R. Goûtez et voyez comme est bon notre Seigneur,Recevez Jésus, livré pour le Salut.Devenez le temple saint du Christ Ressuscité,Devenez le temple saint,Demeure du Sauveur.1. Par ton corps livré, tu prends sur toi la faute,Par ton sang versé, tu laves nos péchés,Par ton cœur blessé, d’où jaillit le Salut,Tu nous as rachetés.2. Jésus, pain du ciel, tu descends dans le monde,Dieu parmi les siens, mystère de l’amour,Tu te rends présent, livré entre nos mains,Près de nous pour toujours.3. Seigneur, tu nous offres, en ce banquet de Noces,Le vin de l’alliance et le pain de la Vie,Nous vivons en toi, comme tu vis en nous,Un seul corps dans l’Esprit.4. En te recevant, nous devenons l’Église,Peuple racheté, prémices du Salut.Par ta charité, tu rassembles en un corpsLes enfants dispersés.5. Qu’il est grand, Seigneur, l’amour dont tu nous aimes,Tu te livres à nous en cette Eucharistie,Sommet de l’amour, don de la Trinité,Tu te fais pain de Vie.", "type_messe": "ORD", "type_chant": "COM", "compositeur": null, "harmonisation": null, "partition": null, "youtube": "https://youtu.be/8mxzk03fRes?si=xdNAIHYiSfUp0gjA", "audios": []}, {"titre": "L'éternel seul est ma lumière", "reference1": "PS_27", "reference2": "CJ_139", "texte": null, "type_messe": "TSN", "type_chant": "PSM", "compositeur": "Franz Joseph haydn", "harmonisation": null, "partition": "https://cantiques.yapper.fr/CJ/CJ_139_partition.html", "youtube": null, "audios": [{"label": "Écouter", "src": "/Medias/Partitions/CJ_139"}]}, {"titre": "Gloria de Lourdes", "reference1": null, "reference2": "A189", "texte": "Gloria, gloria, in excelsis Deo ! (bis)Paix sur la terre aux hommes qu’il aimeNous te louons, nous te bénissons, nous t’adorons,Nous te glorifions, nous te rendons grâce pour ton immense gloire,Seigneur Dieu, roi du ciel, Dieu le Père tout-puissant.Gloria, gloria, in excelsis Deo ! (bis)Seigneur, Fils unique, Jésus-Christ,Seigneur Dieu, Agneau de dieu, le fils du Père ;Toi qui enlèves les péchés du monde, prends pitié de nous ;Toi qui enlèves les péchés du monde, reçois notre prière ;Toi qui es assis à la droite du Père, prends pitié de nous;Gloria, gloria, in excelsis Deo ! (bis)Car toi seul es Saint, toi seul es Seigneur,Toi seul es le très haut ; Jésus-Christ avec le Saint EspritDans la gloire de Dieu le père, AMEN", "type_messe": "NOL", "type_chant": "GLO", "compositeur": null, "harmonisation": null, "partition": "https://choeurs-st-louis.fr/siteChorale/pdfLitur/Gloria_Lourdes.pdf", "youtube": null, "audios": []}, {"titre": "Sanctus Liberia", "reference1": "Sanctus Liberia", "reference2": null, "texte": null, "type_messe": "ORD", "type_chant": "SCT", "compositeur": "Robert Prizeman", "harmonisation": null, "partition": null, "youtube": null, "audios": [{"label": "Sanctus", "src": "media/Audios/Sanctus.aac"}]}, {"titre": "A mfumu", "reference1": null, "reference2": null, "texte": null, "type_messe": "ORD", "type_chant": "KYR", "compositeur": "Fred Olichet Biyela", "harmonisation": "Fred Olichet Biyela", "partition": null, "youtube": null, "audios": [{"label": "Écouter", "src": "/Medias/PartitionsSR002"}]}, {"titre": "Mon âme a soif du dieu vivant", "reference1": "PS_41", "reference2": null, "texte": "Mon âme a soif de Toi, Dieu vivant, Dieu très haut.Prends-moi, pacifie-moi, Seigneur mon Roi.Tu es toute ma joie, ô viens en moi!Mon âme a soif de Dieu,le Dieu vivant ;quand pourrai-je m’avancer,paraître face à Dieu ?Je conduisais vers la maison de mon Dieula multitude en fête,parmi les cris de joieet les actions de grâce.Envoie ta lumière et ta vérité :qu’elles guident mes paset me conduisent à ta montagne sainte,jusqu’en ta demeure.J’avancerai jusqu’à l’autel de Dieu,vers Dieu qui est toute ma joie ;je te rendrai grâce avec ma harpe,Dieu, mon Dieu.", "type_messe": "ORD", "type_chant": "PSM", "compositeur": "Eric Lebrun", "harmonisation": null, "partition": null, "youtube": null, "audios": []}, {"titre": "Saint Esprit, feu d'amour", "reference1": null, "reference2": null, "texte": null, "type_messe": "PTC", "type_chant": "OFR", "compositeur": "Fred Olichet Biyela", "harmonisation": null, "partition": "/Medias/PartitionsSR001", "youtube": null, "audios": []}, {"titre": "A toi la gloire", "reference1": null, "reference2": "M_191", "texte": "Strophe 11. À toi la gloire, ô ressuscité !À toi la victoire pour l'éternité !Brillant de lumière, l'ange est descendu,Il roule la pierre, du tombeau vaincu.RefrainÀ toi la gloire, ô ressuscité !À toi la victoire pour l'éternité !Strophe 22. Vois-le paraître: c'est lui, c'est Jésus,Ton Sauveur, ton Maître ! Oh ! ne doute plus !Sois dans l'allégresse, peuple du Seigneur,Et redis sans cesse que Christ est vainqueur.RefrainÀ toi la gloire, ô ressuscité !À toi la victoire pour l'éternité !Strophe 33. Craindrais-je encore ? Il vit à jamais,Celui que j'adore, le Prince de paix ;Il est ma victoire, mon puissant soutien,Ma vie et ma gloire: non, je ne crains rien.RefrainÀ toi la gloire, ô ressuscité !À toi la victoire pour l'éternité !", "type_messe": "PAQ", "type_chant": "ENV", "compositeur": "G. F. Haendel ", "harmonisation": null, "partition": "media/Partitions/M_191.pdf", "youtube": null, "audios": []}, {"titre": "Humblement dans le silence de mon cœur", "reference1": null, "reference2": "C00035", "texte": "Refrain :Humblement, dans le silence de mon cœur,Je me donne à toi, mon Seigneur !Couplets :Par ton amour, fais-moi demeurer humble et petit devant toi.Enseigne-moi ta sagesse, Ô Dieu, viens habiter mon silence.Entre tes mains, je remets ma vie, ma volonté, tout mon être.Je porte en moi ce besoin d’amour, de me donner, de me livrer sans retour.Vierge Marie, garde mon chemin dans l’abandon, la confiance de l’amour", "type_messe": null, "type_chant": "COM", "compositeur": null, "harmonisation": null, "partition": "Humblement_dans_le_silence.pdf ", "youtube": null, "audios": [{"label": "Refrain", "src": "media/Audios/Humblement-ref.aac"}, {"label": "Couplets — Soprano", "src": "media/Audios/Humblement-couplets-soprane.aac"}, {"label": "Couplets — Alto", "src": "media/Audios/Humblement-couplet-alto.aac"}, {"label": "Couplets — Ténor", "src": "media/Audios/Humblement-couplets-tenor.aac"}, {"label": "Couplets — Basse", "src": "media/Audios/Humblement-couplets-basse.aac"}]}, {"titre": "Alléluia de Pâques", "reference1": null, "reference2": null, "texte": null, "type_messe": "PAQ", "type_chant": "ACC", "compositeur": null, "harmonisation": null, "partition": null, "youtube": null, "audios": [{"label": "Alléluia", "src": "media/Audios/alleluia-paques.aac"}]}], "musiciens": [{"nom": "Dupond", "prenom": "Marc", "date_arrivee": "2020-01-01", "instrument": "GIT", "clocher": 1}, {"nom": "Busson", "prenom": "Valerie", "date_arrivee": "2023-05-03", "instrument": "FLT", "clocher": 1}, {"nom": "Avio", "prenom": "Vino", "date_arrivee": "2001-07-14", "instrument": "ORG", "clocher": 2}, {"nom": "Labelle", "prenom": "Paty", "date_arrivee": "1995-09-18", "instrument": "TBN", "clocher": 1}, {"nom": "Libe", "prenom": "Lula", "date_arrivee": "1999-12-22", "instrument": "PIA", "clocher": 2}, {"nom": "Abé", "prenom": "Lianne", "date_arrivee": "2023-11-26", "instrument": "SAX", "clocher": 2}, {"nom": "Jard", "prenom": "Inola", "date_arrivee": "2025-01-19", "instrument": "JMB", "clocher": 1}, {"nom": "Mbo", "prenom": "Télia", "date_arrivee": "2020-02-15", "instrument": "FTRV", "clocher": 2}, {"nom": "Bazil", "prenom": "Baver", "date_arrivee": "2001-02-02", "instrument": "CLA", "clocher": 1}], "animateurs": [{"nom": "AUBRI", "prenom": "Martine", "date_arrivee": "2023-12-10", "clocher": 2}, {"nom": "CAMUS", "prenom": "Paul", "date_arrivee": "2026-05-04", "clocher": 1}, {"nom": "MALAMBA", "prenom": "Niska", "date_arrivee": "2020-02-01", "clocher": 1}, {"nom": "PERTUI", "prenom": "Perpétu", "date_arrivee": "1998-11-17", "clocher": 2}, {"nom": "VACOUM", "prenom": "Youli", "date_arrivee": "2021-01-01", "clocher": 2}, {"nom": "SENSEY", "prenom": "Josh", "date_arrivee": "2000-09-19", "clocher": 1}, {"nom": "HONI", "prenom": "Liyam", "date_arrivee": "2020-03-01", "clocher": 1}, {"nom": "BALA", "prenom": "Bani", "date_arrivee": "2019-01-15", "clocher": 1}, {"nom": "MOUNDI", "prenom": "Victoire", "date_arrivee": null, "clocher": null}], "instruments": {"GIT": "Guitare", "FLT": "Flûte à bec", "ORG": "Orgue", "TBN": "Trombone", "PIA": "Piano", "SAX": "Saxophone", "JMB": "Djembé", "TRV": "Flûte traversière", "CLA": "Clavier", "FTRV": "Flûte traversière"}, "type_messe": {"ORD": "Ordinaire", "NOL": "Noël", "CRM": "Carême", "PAQ": "Pâques", "PTC": "Pentecôte", "TSN": "Toussaint", "BTM": "Baptême", "ADG": "Action de grâce", "MAR": "Mariage", "BEN": "Bénédiction Nuptiale", "OBQ": "Obsèques", "COM": "Première Communion", "PRF": "Profession de foi", "ODN": "Ordination"}, "type_chant": {"ENT": "Ouverture", "KYR": "Kyrié", "GLO": "Gloria", "PSM": "Psaume", "ACC": "Acclamation", "CDO": "Credo", "PU": "Prière Universelle", "OFR": "Offertoire", "SCT": "Sanctus", "ANM": "Anamnèse", "DOXO": "Doxologie", "NP": "Notre Père", "AGD": "Agnus Dei", "COM": "Communion", "ATG": "Action de Grâce", "ENV": "Envoi", "MAR": "Chant à Marie"}, "ordinaire_messe": {"JEA": "Messe de St Jean", "SFX": "Messe de St François Xavier", "BBR": "Messe du Bon Berger", "TRT": "Messe de la Trinité", "REU": "Messe de La réunion", "PLR": "Messe des Pélerins", "PAU": "Messe de St Paul", "PAR": "Messe Du Partage"}, "clochers": {"1": "Cesson_Sevigne", "2": "Thorigne_Fouillard"}};
/* ============================================================
   GESTOFFICE — application de préparation des chants liturgiques
   Application autonome (aucun serveur). Les données sont stockées
   dans le navigateur (localStorage) à partir du jeu de données fourni.
   ============================================================ */

/* ---------------- Connexion Supabase ---------------- */
const SUPABASE_URL = 'https://dbodbhoczptphkhbmkaw.supabase.co';
const SUPABASE_KEY = 'sb_publishable_XXyIYSPlsItT_Vu1IsNfNA_gBAfv-hu';
const sb = window.supabase.createClient(SUPABASE_URL, SUPABASE_KEY);

let CURRENT_USER = null;   // objet utilisateur Supabase Auth (ou null si déconnecté)
let CURRENT_PROFILE = null; // ligne correspondante de la table "profiles" (contient le rôle)

/* Récupère le profil (et donc le rôle) de l'utilisateur actuellement connecté */
async function fetchCurrentProfile() {
  if (!CURRENT_USER) { CURRENT_PROFILE = null; return null; }
  const { data, error } = await sb
    .from('profiles')
    .select('*')
    .eq('id', CURRENT_USER.id)
    .single();
  if (error) { console.error('Erreur chargement profil :', error); CURRENT_PROFILE = null; return null; }
  CURRENT_PROFILE = data;
  return data;
}

/* Inscription : crée un compte + déclenche automatiquement la création
   du profil (rôle "visiteur" par défaut) via le trigger côté base. */
async function signUp(email, password) {
  const { data, error } = await sb.auth.signUp({ email, password });
  if (error) { toast('Erreur inscription : ' + error.message); return false; }
  if (data.user && !data.session) {
    toast('Compte créé ! Vérifie ta boîte mail pour confirmer ton adresse avant de te connecter.');
    return 'confirm-email';
  }
  return true;
}

async function signIn(email, password) {
  const { data, error } = await sb.auth.signInWithPassword({ email, password });
  if (error) { toast('Erreur connexion : ' + error.message); return false; }
  CURRENT_USER = data.user;
  await fetchCurrentProfile();
  ROLE = CURRENT_PROFILE ? CURRENT_PROFILE.role : null;
  return true;
}

async function signOutSupabase() {
  await sb.auth.signOut();
  CURRENT_USER = null;
  CURRENT_PROFILE = null;
  ROLE = null;
}

/* Au chargement de la page : vérifie si une session est déjà active
   (utilisateur déjà connecté lors d'une visite précédente). */
async function restoreSession() {
  const { data } = await sb.auth.getSession();
  if (data.session) {
    CURRENT_USER = data.session.user;
    await fetchCurrentProfile();
    ROLE = CURRENT_PROFILE ? CURRENT_PROFILE.role : null;
  }
}

/* ---------------- Chargement des données réelles depuis Supabase ----------------
   Remplace le contenu de DB (jusqu'ici basé sur localStorage/SEED) par les
   vraies données de la base, en conservant EXACTEMENT la même forme d'objets
   que le reste de l'application attend (ex: DB.instruments = {CODE: 'Libellé'},
   musicien.clocher = id numérique, etc.) — ainsi tout le code de rendu existant
   continue de fonctionner sans autre modification. */

function rowsToMap(rows) {
  const map = {};
  (rows || []).forEach(r => { map[r.code] = r.libelle; });
  return map;
}
function clochersToMap(rows) {
  const map = {};
  (rows || []).forEach(r => { map[r.id] = r.nom; });
  return map;
}
function schemaFromDefs(defs) {
  const schema = { chants: [], musiciens: [], animateurs: [], calendrier: [] };
  (defs || []).forEach(d => {
    if (schema[d.entity]) schema[d.entity].push({ key: d.key, label: d.label, type: d.type });
  });
  return schema;
}

/* Calcule l'URL publique d'un fichier stocké dans le bucket "media" à partir
   de son chemin relatif (ex: "partitions/M_191.pdf"). Retourne null si le
   chemin est déjà une URL complète (http...) ou vide. */
function mediaUrl(path) {
  if (!path) return null;
  if (/^https?:\/\//i.test(path)) return path; // déjà une URL externe complète
  const { data } = sb.storage.from('medias').getPublicUrl(path);
  return data ? data.publicUrl : null;
}

async function loadDBFromSupabase() {
  const [
    { data: chants, error: e1 },
    { data: musiciens, error: e2 },
    { data: animateurs, error: e3 },
    { data: programmes, error: e4 },
    { data: calendrier, error: e5 },
    { data: noOffice, error: e6 },
    { data: instruments, error: e7 },
    { data: typeMesse, error: e8 },
    { data: typeChant, error: e9 },
    { data: ordinaireMesse, error: e10 },
    { data: clochers, error: e11 },
    { data: customDefs, error: e12 },
  ] = await Promise.all([
    sb.from('chants').select('*').order('id'),
    sb.from('musiciens').select('*').order('id'),
    sb.from('animateurs').select('*').order('id'),
    sb.from('programmes').select('*').order('id'),
    sb.from('calendrier').select('*').order('id'),
    sb.from('no_office').select('*').order('id'),
    sb.from('instruments').select('*'),
    sb.from('type_messe').select('*'),
    sb.from('type_chant').select('*'),
    sb.from('ordinaire_messe').select('*'),
    sb.from('clochers').select('*'),
    sb.from('custom_field_defs').select('*'),
  ]);

  const errors = [e1,e2,e3,e4,e5,e6,e7,e8,e9,e10,e11,e12].filter(Boolean);
  if (errors.length) {
    console.error('Erreurs de chargement Supabase :', errors);
    toast('Erreur de chargement des données. Vérifie ta connexion.');
    return false;
  }

  DB.chants = chants.map(c => ({ ...c, texte: c.texte })); // conservé tel quel
  DB.musiciens = musiciens.map(m => ({ ...m, clocher: m.clocher_id }));
  DB.animateurs = animateurs.map(a => ({ ...a, clocher: a.clocher_id }));
  DB.programmes = programmes;
  DB.calendrier = calendrier.map(c => ({ ...c, lieu: c.lieu_id, organiste: c.organiste_id, chantre: c.chantre_id, programmeId: c.programme_id }));
  DB.noOffice = noOffice.map(n => ({ ...n, lieu: n.lieu_id }));
  DB.instruments = rowsToMap(instruments);
  DB.type_messe = rowsToMap(typeMesse);
  DB.type_chant = rowsToMap(typeChant);
  DB.ordinaire_messe = rowsToMap(ordinaireMesse);
  DB.clochers = clochersToMap(clochers);
  DB.schema = schemaFromDefs(customDefs);
  DB.notifications = []; // remplacé plus tard par le mécanisme de pop-up dédié

  return true;
}


const LS_KEY = 'cantoral_db_v3';
const ROLES = {
  visiteur:   { label: 'Visiteur',   desc: 'Consultation, téléchargement et impression uniquement.' },
  animateur:  { label: 'Animateur',  desc: 'Ajoute et modifie chants, programmes et calendriers.' },
  cp:         { label: 'Conseil paroissial', desc: 'Valide les programmes soumis par les animateurs.' },
  webmaster:  { label: 'WebMaster',  desc: 'Administration complète, maintenance et sécurité de l’outil.' },
};

const SLOTS = [
  { code: 'ENT',  label: "Chant d'ouverture" },
  { code: 'KYR',  label: 'Préparation pénitentielle' },
  { code: 'GLO',  label: 'Gloria' },
  { code: 'PSM',  label: 'Psaume' },
  { code: 'ACC',  label: 'Acclamation (Alléluia)' },
  { code: 'CDO',  label: 'Credo' },
  { code: 'PU',   label: 'Prière universelle' },
  { code: 'OFR',  label: 'Offertoire' },
  { code: 'SCT',  label: 'Sanctus' },
  { code: 'ANM',  label: 'Anamnèse' },
  { code: 'DOXO', label: 'Doxologie' },
  { code: 'NP',   label: 'Notre Père' },
  { code: 'AGD',  label: 'Agnus Dei' },
  { code: 'COM',  label: 'Communion' },
  { code: 'ATG',  label: 'Action de grâce' },
  { code: 'ENV',  label: 'Envoi' },
];
const ORDINAIRE_SLOTS = ['KYR', 'GLO', 'ACC', 'SCT', 'ANM', 'AGD'];
const DEFAULT_MENTIONS = { CDO: 'Récité', NP: 'Récité', DOXO: 'Simple' };

/* ---------------- Persistence ---------------- */
function loadDB() {
  const raw = localStorage.getItem(LS_KEY);
  if (raw) {
    try {
      const db = JSON.parse(raw);
      db.schema = db.schema || { chants: [], musiciens: [], animateurs: [], calendrier: [] };
      db.noOffice = db.noOffice || [];
      return db;
    } catch (e) { /* fall through to seed */ }
  }
  const db = {
    chants: SEED.chants.map((c, i) => ({ id: i + 1, ...c })),
    musiciens: SEED.musiciens.map((m, i) => ({ id: i + 1, ...m })),
    animateurs: SEED.animateurs.map((a, i) => ({ id: i + 1, ...a })),
    instruments: SEED.instruments,
    type_messe: SEED.type_messe,
    type_chant: SEED.type_chant,
    ordinaire_messe: SEED.ordinaire_messe,
    clochers: SEED.clochers,
    programmes: [],
    calendrier: [],
    notifications: [],
    schema: { chants: [], musiciens: [], animateurs: [], calendrier: [] },
    noOffice: [],
    nextChantId: SEED.chants.length + 1,
    nextMusicienId: SEED.musiciens.length + 1,
    nextAnimateurId: SEED.animateurs.length + 1,
    nextProgId: 1,
  };
  saveDB(db);
  return db;
}
function saveDB(db) { localStorage.setItem(LS_KEY, JSON.stringify(db)); }

let DB = loadDB();
let ROLE = null; // déterminé de façon asynchrone par restoreSession() au chargement

function persist() { saveDB(DB); }

/* ---------------- Permissions ---------------- */
function can(action) {
  if (!ROLE) return false;
  if (ROLE === 'visiteur') return false;
  if (action === 'validate') return ROLE === 'cp' || ROLE === 'webmaster';
  if (action === 'admin') return ROLE === 'webmaster';
  if (action === 'manage-personnes') return ROLE === 'cp';
  return true; // animateur, cp, webmaster can edit
}

/* ---------------- Router ---------------- */
window.addEventListener('hashchange', render);
window.addEventListener('DOMContentLoaded', async () => {
  render(); // affiche un état initial (écran de connexion) pendant la vérification
  await restoreSession();
  if (ROLE) await loadDBFromSupabase();
  render();
  if (ROLE) showVacantPopupIfNeeded();
});

function nav(hash) { location.hash = hash; }
function goBack(fallback) {
  if (window.history.length > 1) { window.history.back(); }
  else { nav(fallback || '#/accueil'); }
}

function currentRoute() {
  const h = location.hash.replace(/^#\/?/, '');
  const parts = h.split('/').filter(Boolean);
  return { page: parts[0] || 'accueil', param: parts[1] };
}

/* ---------------- Utilities ---------------- */
function esc(s) {
  if (s === null || s === undefined) return '';
  return String(s).replace(/[&<>"']/g, c => ({ '&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;' }[c]));
}
function parseLocalDate(iso) {
  if (!iso) return null;
  const m = /^(\d{4})-(\d{2})-(\d{2})/.exec(iso);
  if (!m) return new Date(iso);
  return new Date(Number(m[1]), Number(m[2])-1, Number(m[3]));
}
function fmtDate(iso) {
  if (!iso) return '—';
  const d = parseLocalDate(iso);
  if (isNaN(d)) return iso;
  return d.toLocaleDateString('fr-FR', { day: '2-digit', month: 'long', year: 'numeric' });
}
function typeMesseLabel(code) { return DB.type_messe[code] || code || '—'; }
function typeChantLabel(code) { return DB.type_chant[code] || code || '—'; }
function clocherLabel(id) { return DB.clochers[String(id)] || DB.clochers[id] || '—'; }
function instrumentLabel(code) { return DB.instruments[code] || code || '—'; }

function isLocalPath(p) { return !!p && !/^https?:/i.test(p); }

function renderAudios(audios) {
  if (!audios || !audios.length) return `<p class="muted">Aucun audio disponible pour ce chant.</p>`;
  return audios.map(a => {
    if (!a.src) return `<p class="muted">${esc(a.label)} — fichier non fourni.</p>`;
    const url = isLocalPath(a.src) ? mediaUrl(a.src) : a.src;
    if (!url) return `<p class="muted">${esc(a.label)} — fichier non fourni.</p>`;
    if (isLocalPath(a.src)) {
      return `<div style="margin-bottom:12px;">
        <div class="muted" style="font-size:12px;margin-bottom:4px;">${esc(a.label)}</div>
        <audio controls style="width:100%;height:34px;" preload="none">
          <source src="${esc(url)}">
          Votre navigateur ne peut pas lire ce fichier audio.
        </audio>
      </div>`;
    }
    return `<p style="margin-bottom:8px;">♪ <a href="${esc(url)}" target="_blank">${esc(a.label)}</a></p>`;
  }).join('');
}

function renderPartition(p) {
  if (!p) return `<p class="muted">Aucune partition référencée.</p>`;
  if (/^https?:/i.test(p)) {
    return `<p><a href="${esc(p)}" target="_blank">📄 Voir la partition (lien externe) ↗</a></p>`;
  }
  const url = mediaUrl(p);
  if (/\.pdf$/i.test(p) && url) {
    return `<p><a href="${esc(url)}" target="_blank">📄 Ouvrir la partition (PDF)</a></p>`;
  }
  return `<p class="muted">Partition référencée mais fichier non disponible : <span style="font-family:monospace;font-size:12px;">${esc(p)}</span></p>`;
}

function normStr(s) {
  return (s || '').toString().toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '').replace(/[^a-z0-9\s]/g, ' ').replace(/\s+/g, ' ').trim();
}
function levenshtein(a, b) {
  const m = a.length, n = b.length;
  if (!m) return n; if (!n) return m;
  let prev = Array.from({length: n+1}, (_, i) => i);
  for (let i = 1; i <= m; i++) {
    const cur = [i];
    for (let j = 1; j <= n; j++) {
      cur[j] = a[i-1] === b[j-1] ? prev[j-1] : 1 + Math.min(prev[j-1], prev[j], cur[j-1]);
    }
    prev = cur;
  }
  return prev[n];
}
function fuzzyWordMatch(qw, tw) {
  if (qw === tw) return true;
  if (qw.length < 4 || tw.length < 4) return false;
  if (tw.includes(qw) || qw.includes(tw)) return true;
  const dist = levenshtein(qw, tw);
  return dist <= Math.max(1, Math.floor(Math.min(qw.length, tw.length) * 0.3));
}
function fuzzyMatch(text, query) {
  const normText = normStr(text);
  const normQuery = normStr(query);
  if (!normQuery) return true;
  if (!normText) return false;
  if (normText.includes(normQuery)) return true;
  const textWords = normText.split(' ').filter(Boolean);
  const queryWords = normQuery.split(' ').filter(Boolean);
  return queryWords.every(qw => textWords.some(tw => fuzzyWordMatch(qw, tw)));
}
function scoreOverlap(text, ref) {
  if (!text || !ref) return 0;
  const norm = s => s.toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g, '').replace(/[^a-z0-9\s]/g, ' ').split(/\s+/).filter(w => w.length > 3);
  const a = new Set(norm(text));
  const b = norm(ref);
  if (!b.length) return 0;
  let hits = 0;
  b.forEach(w => { if (a.has(w)) hits++; });
  return Math.round((hits / b.length) * 100);
}

function toast(msg) {
  let t = document.getElementById('toast');
  if (!t) {
    t = document.createElement('div');
    t.id = 'toast';
    t.style.cssText = 'position:fixed;bottom:22px;left:50%;transform:translateX(-50%);background:var(--ink);color:#fff;padding:10px 18px;border-radius:8px;font-size:13px;z-index:999;box-shadow:0 4px 16px rgba(0,0,0,.2);';
    document.body.appendChild(t);
  }
  t.textContent = msg;
  t.style.opacity = '1';
  clearTimeout(window._toastTimer);
  window._toastTimer = setTimeout(() => { t.style.opacity = '0'; }, 2600);
}

/* ---------------- Render root ---------------- */
function render() {
  const app = document.getElementById('app');
  if (!ROLE) {
    ['vacant-popup-overlay', 'incomplete-chants-overlay'].forEach(id => { const el = document.getElementById(id); if (el) el.remove(); });
    app.innerHTML = renderLogin();
    return;
  }
  const { page, param } = currentRoute();
  if (page === 'chants' && !param && window._lastPage !== 'chants') {
    setTimeout(showIncompleteChantsPopupIfNeeded, 0);
  }
  window._lastPage = page;
  app.innerHTML = `
    ${renderSidebar(page)}
    <main>
      ${page !== 'accueil' ? `<a class="back-link no-print" onclick="nav('#/accueil')">← Retour à l’accueil</a>` : ''}
      ${renderPage(page, param)}
    </main>
  `;
}

function renderLogin() {
  return `
  <div class="login-wrap">
    <div class="login-card">
      <div class="brand" style="margin-bottom:22px;">
        <div class="brand-mark">C</div>
        <div class="brand-text" style="color:var(--ink);">GestOffice<span style="color:var(--ink-soft);">Préparation des chants</span></div>
      </div>
      <div id="auth-mode-signin">
        <h2>Connexion</h2>
        <p class="muted" style="margin-top:-6px;margin-bottom:16px;">Connecte-toi avec ton compte GestOffice.</p>
        <div class="field"><label>Email</label><input id="auth-email" type="email" autocomplete="email"></div>
        <div class="field"><label>Mot de passe</label><input id="auth-password" type="password" autocomplete="current-password"></div>
        <button class="primary" style="width:100%;margin-top:10px;" onclick="handleSignIn()">Se connecter</button>
        <p class="muted" style="margin-top:14px;font-size:13px;text-align:center;">
          Pas encore de compte ? <a href="#" onclick="toggleAuthMode();return false;">Créer un compte</a>
        </p>
        <div id="auth-error" style="color:#8a2b2b;font-size:13px;margin-top:8px;"></div>
      </div>
      <div id="auth-mode-signup" style="display:none;">
        <h2>Créer un compte</h2>
        <p class="muted" style="margin-top:-6px;margin-bottom:16px;">Ton compte sera créé avec le profil "Visiteur" par défaut ; un webmaster devra ensuite t'attribuer le bon rôle.</p>
        <div class="field"><label>Email</label><input id="auth-email-su" type="email" autocomplete="email"></div>
        <div class="field"><label>Mot de passe</label><input id="auth-password-su" type="password" autocomplete="new-password"></div>
        <button class="primary" style="width:100%;margin-top:10px;" onclick="handleSignUp()">Créer mon compte</button>
        <p class="muted" style="margin-top:14px;font-size:13px;text-align:center;">
          Déjà un compte ? <a href="#" onclick="toggleAuthMode();return false;">Se connecter</a>
        </p>
        <div id="auth-error-su" style="color:#8a2b2b;font-size:13px;margin-top:8px;"></div>
      </div>
    </div>
  </div>`;
}

function toggleAuthMode() {
  const signin = document.getElementById('auth-mode-signin');
  const signup = document.getElementById('auth-mode-signup');
  const showSignup = signin.style.display !== 'none';
  signin.style.display = showSignup ? 'none' : '';
  signup.style.display = showSignup ? '' : 'none';
}

async function handleSignIn() {
  const email = document.getElementById('auth-email').value.trim();
  const password = document.getElementById('auth-password').value;
  const errEl = document.getElementById('auth-error');
  errEl.textContent = '';
  if (!email || !password) { errEl.textContent = 'Renseigne ton email et ton mot de passe.'; return; }
  const ok = await signIn(email, password);
  if (!ok) { errEl.textContent = 'Email ou mot de passe incorrect.'; return; }
  await bootstrapAfterLogin();
}

async function handleSignUp() {
  const email = document.getElementById('auth-email-su').value.trim();
  const password = document.getElementById('auth-password-su').value;
  const errEl = document.getElementById('auth-error-su');
  errEl.textContent = '';
  if (!email || !password) { errEl.textContent = 'Renseigne un email et un mot de passe.'; return; }
  if (password.length < 6) { errEl.textContent = 'Le mot de passe doit faire au moins 6 caractères.'; return; }
  const result = await signUp(email, password);
  if (!result) { errEl.textContent = "Impossible de créer le compte."; return; }
  if (result === 'confirm-email') { toggleAuthMode(); return; }
  await bootstrapAfterLogin();
}

/* Appelé juste après une connexion réussie : charge les vraies données
   depuis Supabase, recharge la vue et affiche les popups habituelles
   (dates vacantes, programmes validés...). */
async function bootstrapAfterLogin() {
  await loadDBFromSupabase();
  nav('#/accueil');
  render();
  showVacantPopupIfNeeded();
}
function chantHasMedia(c) {
  return !!c.partition || (c.audios && c.audios.length > 0) || !!c.youtube;
}
function chantIsDocComplete(c) {
  return chantHasMedia(c) && !!c.texte;
}
function computeIncompleteChants() {
  return DB.chants.filter(c => !chantIsDocComplete(c)).map(c => {
    const missing = [];
    if (!chantHasMedia(c)) missing.push('partition, audio ou vidéo');
    if (!c.texte) missing.push('texte');
    return { chant: c, missing: missing.join(' et ') };
  });
}
function showIncompleteChantsPopupIfNeeded() {
  if (!can('edit')) return;
  const list = computeIncompleteChants();
  if (!list.length) return;
  const existing = document.getElementById('incomplete-chants-overlay');
  if (existing) existing.remove();
  const overlay = document.createElement('div');
  overlay.id = 'incomplete-chants-overlay';
  overlay.style.cssText = 'position:fixed;inset:0;background:rgba(43,36,29,0.5);z-index:997;display:flex;align-items:center;justify-content:center;';
  overlay.innerHTML = `
    <div style="background:var(--paper);border:1px solid var(--line-strong);border-radius:16px;box-shadow:0 12px 40px rgba(0,0,0,0.28);padding:24px;max-width:440px;width:90%;max-height:80vh;overflow:auto;">
      <div style="text-align:center;">
        <div style="font-size:28px;line-height:1;margin-bottom:8px;">⚠</div>
        <b style="font-family:'Fraunces',serif;font-size:18px;">Chants incomplets</b>
        <p class="muted" style="margin:6px 0 14px;font-size:13px;">${list.length} chant(s) sans partition/audio/vidéo, ou sans texte. Cliquez sur un chant pour le compléter.</p>
      </div>
      <div class="chant-list">
        ${list.map(({chant,missing}) => `
          <div class="chant-row" onclick="document.getElementById('incomplete-chants-overlay').remove();nav('#/chants/${chant.id}')" style="cursor:pointer;">
            <div class="rib" style="background:var(--gold)"></div>
            <div style="flex:1;">
              <div class="chant-title" style="font-size:14px;">${esc(chant.titre)}</div>
              <div class="chant-meta">Manque : ${esc(missing)}</div>
            </div>
          </div>`).join('')}
      </div>
      <button class="primary" style="margin-top:16px;width:100%;" onclick="document.getElementById('incomplete-chants-overlay').remove()">Fermer</button>
    </div>`;
  document.body.appendChild(overlay);
}
function showVacantPopupIfNeeded() {
  if (!can('edit')) return;
  const count = computeVacantDates(8).length;
  if (!count) return;
  const existing = document.getElementById('vacant-popup-overlay');
  if (existing) existing.remove();
  const overlay = document.createElement('div');
  overlay.id = 'vacant-popup-overlay';
  overlay.style.cssText = 'position:fixed;inset:0;background:rgba(43,36,29,0.5);z-index:997;display:flex;align-items:center;justify-content:center;';
  overlay.innerHTML = `
    <div style="background:var(--paper);border:1px solid var(--line-strong);border-radius:16px;box-shadow:0 12px 40px rgba(0,0,0,0.28);padding:26px;max-width:360px;width:90%;text-align:center;">
      <div style="font-size:30px;line-height:1;margin-bottom:10px;">⚠</div>
      <b style="font-family:'Fraunces',serif;font-size:19px;">Dates vacantes détectées</b>
      <p class="muted" style="margin:8px 0 0;font-size:13.5px;">${count} créneau(x) samedi/dimanche sans office (ou incomplet) dans les 8 prochaines semaines.</p>
      <div class="row" style="margin-top:18px;justify-content:center;gap:10px;">
        <button class="ghost" onclick="document.getElementById('vacant-popup-overlay').remove()">Plus tard</button>
        <button class="primary" onclick="document.getElementById('vacant-popup-overlay').remove();nav('#/dates-vacantes')">Vérifier les dates vacantes</button>
      </div>
    </div>`;
  document.body.appendChild(overlay);
}
async function logout() {
  await signOutSupabase();
  location.hash = '';
  ['vacant-popup-overlay', 'incomplete-chants-overlay'].forEach(id => { const el = document.getElementById(id); if (el) el.remove(); });
  render();
}
function confirmLogout() {
  if (!confirm('Voulez-vous vraiment vous déconnecter ?')) return;
  window._linkingOffice = null;
  window._prefillCalDate = null; window._prefillCalLieu = null;
  window._prefillProgDate = null; window._prefillProgLieu = null;
  window._editCalId = null;
  logout();
  toast('Vous avez été déconnecté');
}

function renderSidebar(active) {
  const item = (hash, icon, label) => `
    <a class="nav-item ${active === hash ? 'active' : ''}" onclick="nav('#/${hash}')">
      <span class="nav-icon">${icon}</span>${label}
    </a>`;
  return `
  <div class="sidebar no-print">
    <div class="brand">
      <div class="brand-mark">C</div>
      <div class="brand-text">GestOffice<span>Chants &amp; offices</span></div>
    </div>
    <nav>
      ${item('accueil', '⌂', 'Accueil')}
      ${item('chants-liste', '☰', 'Afficher les chants')}
      ${item('chants', '♪', 'Rechercher un chant')}
      ${item('programmes', '❧', 'Afficher les programmes')}
      ${item('calendrier', '▤', 'Calendrier')}
      ${can('edit') ? item('dates-vacantes', '⚠', 'Dates vacantes') : ''}
      <div class="nav-sep">Équipes</div>
      ${item('animateurs', '☙', 'Animateurs')}
      ${item('musiciens', '♫', 'Musiciens')}
      ${can('admin') ? `<div class="nav-sep">Administration</div>${item('admin', '⚙', 'WebMaster')}` : ''}
    </nav>
    <div class="sidebar-foot">
      <div class="role-pill"><span class="role-dot"></span>${ROLES[ROLE].label}</div>
      <button class="ghost" style="width:100%;margin-top:8px;color:#EBDACE;border-color:rgba(255,255,255,0.18);" onclick="confirmLogout()">⏻ Déconnexion</button>
      <span class="switch-link" onclick="logout()">Changer de profil</span>
    </div>
  </div>`;
}

function renderPage(page, param) {
  switch (page) {
    case 'accueil': return pageAccueil();
    case 'chants-liste': return pageChantsListe();
    case 'chants': return param ? pageChantDetail(param) : pageChants();
    case 'chant-nouveau': return pageChantForm();
    case 'chant-edit': return pageChantForm(param);
    case 'programmes': return param ? pageProgrammeDetail(param) : pageProgrammes();
    case 'programme-nouveau': return pageProgrammeForm();
    case 'calendrier': return pageCalendrier();
    case 'dates-vacantes': return pageDatesVacantes();
    case 'animateurs': return pagePersonnes('animateurs');
    case 'musiciens': return pagePersonnes('musiciens');
    case 'admin': return can('admin') ? pageAdmin() : pageForbidden();
    default: return pageAccueil();
  }
}

function pageForbidden() {
  return `<div class="empty"><h2>Accès non autorisé</h2><p class="muted">Votre profil ne permet pas d’accéder à cette page.</p></div>`;
}
/* ---------------- Accueil ---------------- */
function pageAccueil() {
  const keyOf = (date, lieu) => date + '|' + (lieu || '');
  const seen = new Map();
  DB.calendrier.filter(o => o.date >= todayISO()).forEach(o => seen.set(keyOf(o.date, o.lieu), { date: o.date, lieu: o.lieu }));
  DB.programmes.filter(p => p.date >= todayISO()).forEach(p => { const k = keyOf(p.date, p.lieu); if (!seen.has(k)) seen.set(k, { date: p.date, lieu: p.lieu }); });
  const upcoming = [...seen.values()].sort((a,b) => a.date.localeCompare(b.date)).slice(0, 5);
  return `
  <div class="page-head">
    <div>
      <div class="crumb">Tableau de bord</div>
      <h1>Bonjour, ${ROLES[ROLE].label}</h1>
      <p class="muted">${DB.chants.length} chants au répertoire · ${DB.programmes.length} programme(s) · ${DB.animateurs.length} animateurs · ${DB.musiciens.length} musiciens</p>
    </div>
  </div>
  <div class="grid3" style="margin-bottom:22px;">
    <div class="card" style="cursor:pointer" onclick="nav('#/chants')">
      <div class="badge wine">Lot 1</div>
      <h3 style="margin-top:10px;">Rechercher un chant</h3>
      <p class="muted">Par titre, référence ou texte, dans le répertoire interne ou externe.</p>
    </div>
    <div class="card" style="cursor:pointer" onclick="nav('#/programme-nouveau')">
      <div class="badge gold">Lot 1 / 3</div>
      <h3 style="margin-top:10px;">Générer un programme</h3>
      <p class="muted">Propose les chants les mieux adaptés à la date et au type de messe.</p>
    </div>
    <div class="card" style="cursor:pointer" onclick="nav('#/calendrier')">
      <div class="badge sage">Lot 2 / 3</div>
      <h3 style="margin-top:10px;">Calendrier d’animation</h3>
      <p class="muted">Équipes, horaires et lieux sur une période donnée.</p>
    </div>
  </div>
  <div class="card">
    <h3>Prochaines célébrations</h3>
    ${upcoming.length ? upcoming.map(({date, lieu}) => {
      const office = DB.calendrier.find(o => o.date === date && String(o.lieu||'') === String(lieu||''));
      const prog = office ? findProgrammeForOffice(office) : DB.programmes.find(p => p.date === date && String(p.lieu||'') === String(lieu||''));
      const clickAttr = prog ? `onclick="nav('#/programmes/${prog.id}')"` : `onclick="goToOfficeProgramme('${date}','${lieu||''}')"`;
      const lieuText = office ? `${esc(office.horaire||'')} · ${clocherLabel(office.lieu)}` : (lieu ? clocherLabel(lieu) : '');
      return `
      <div class="row" style="justify-content:space-between;padding:9px 0;border-bottom:1px solid var(--line);cursor:pointer;" ${clickAttr}>
        <div><b>${fmtDate(date)}</b><div class="muted">${lieuText}${prog ? ' · ' + esc(prog.titre) : ''}</div></div>
        <span class="${prog ? 'badge ' + (prog.statut==='validé' ? 'sage' : 'gray') : 'muted'}">${prog ? prog.statut : 'aucun programme'}</span>
      </div>`;
    }).join('') : `<p class="muted">Aucune célébration à venir. <a onclick="nav('#/programme-nouveau')">Créer un programme</a> ou <a onclick="nav('#/calendrier')">ajouter un office</a>.</p>`}
  </div>`;
}
function toLocalISODate(d) {
  const y = d.getFullYear(), m = String(d.getMonth()+1).padStart(2,'0'), day = String(d.getDate()).padStart(2,'0');
  return `${y}-${m}-${day}`;
}
function todayISO() { return toLocalISODate(new Date()); }
function getWeekendDates(weeksAhead) {
  const out = [];
  const start = new Date();
  start.setHours(0,0,0,0);
  for (let i = 0; i < 7 * weeksAhead; i++) {
    const d = new Date(start.getTime() + i * 86400000);
    const day = d.getDay(); // 0 = dimanche, 6 = samedi
    if (day === 0 || day === 6) out.push(toLocalISODate(d));
  }
  return out;
}
function computeVacantDates(weeksAhead) {
  const dates = getWeekendDates(weeksAhead || 8);
  const vacant = [];
  dates.forEach(date => {
    Object.keys(DB.clochers).forEach(lieu => {
      const office = DB.calendrier.find(o => o.date === date && String(o.lieu) === String(lieu));
      const marked = DB.noOffice.some(n => n.date === date && String(n.lieu) === String(lieu));
      if (marked) return;
      if (!office) { vacant.push({ date, lieu, officeId: null, incomplete: false }); return; }
      if (!office.organiste || !office.chantre) { vacant.push({ date, lieu, officeId: office.id, incomplete: true }); }
    });
  });
  return vacant.sort((a,b) => a.date.localeCompare(b.date));
}
function markNoOffice(date, lieu) {
  DB.noOffice.push({ date, lieu });
  persist(); toast('Marqué « pas d’office »'); render();
}
function unmarkNoOffice(date, lieu) {
  DB.noOffice = DB.noOffice.filter(n => !(n.date === date && String(n.lieu) === String(lieu)));
  persist(); toast('Retiré de la liste'); render();
}
function goToVacantOffice(date, lieu) {
  window._prefillCalDate = date;
  window._prefillCalLieu = lieu;
  nav('#/calendrier');
}
function goToEditOffice(officeId) {
  window._editCalId = officeId;
  nav('#/calendrier');
}
function goToOfficeProgramme(date, lieu) {
  if (can('edit')) {
    toast('Aucun programme pour cette date/ce lieu — création en cours');
    window._prefillProgDate = date;
    window._prefillProgLieu = lieu || null;
    nav('#/programme-nouveau');
  } else {
    toast('Aucun programme n’a encore été préparé pour cette date');
  }
}

/* ---------------- Chants : liste ---------------- */
function pageChantsListe() {
  return `
  <div class="page-head">
    <div>
      <div class="crumb">Répertoire</div>
      <h1>Afficher les chants</h1>
      <p class="muted">Sommaire de tous les chants en base, triés par ordre alphabétique.</p>
    </div>
    ${can('edit') ? `<button class="primary" onclick="nav('#/chant-nouveau')">+ Ajouter un chant</button>` : ''}
  </div>
  <div class="card" style="max-width:640px;margin-bottom:18px;">
    <h3>Recherche rapide</h3>
    <div class="grid2">
      <div class="field"><label>Titre du chant</label><input id="lf-titre" value="${esc(window._listeTitre||'')}" placeholder="Ex. Sanctus"></div>
      <div class="field"><label>Référence du chant</label><input id="lf-ref" value="${esc(window._listeRef||'')}" placeholder="Ex. PS_27"></div>
    </div>
    <div class="row">
      <button class="primary" onclick="runSearchChantsListe()">Soumettre</button>
      ${(window._listeTitre||window._listeRef) ? `<button class="ghost" onclick="clearSearchChantsListe()">Réinitialiser</button>` : ''}
    </div>
  </div>
  <div id="chants-liste-zone">${renderChantsListeTable(filterChantsListe())}</div>
  `;
}
function filterChantsListe() {
  const titre = window._listeTitre || '';
  const ref = window._listeRef || '';
  let list = DB.chants;
  if (titre) list = list.filter(c => fuzzyMatch(c.titre, titre));
  if (ref) list = list.filter(c => fuzzyMatch([c.reference1, c.reference2].filter(Boolean).join(' '), ref));
  return [...list].sort((a,b) => a.titre.localeCompare(b.titre, 'fr'));
}
function onlineSearchLinksBlock(q, title) {
  return `
  <div class="card" style="margin-top:16px;">
    <h3 style="font-size:15px;">${title}</h3>
    <div class="row" style="gap:10px;">
      <a class="btn" target="_blank" href="https://www.youtube.com/results?search_query=${encodeURIComponent(q+' chant liturgique catholique')}">Chercher une vidéo sur YouTube ↗</a>
      <a class="btn" target="_blank" href="https://www.google.com/search?q=${encodeURIComponent(q+' partition chant liturgique')}">Chercher une partition ↗</a>
    </div>
  </div>`;
}
function renderChantsListeTable(sorted) {
  const q = [window._listeTitre, window._listeRef].filter(Boolean).join(' ');
  if (!sorted.length) {
    setTimeout(() => fetchOnlineSuggestions(q, 'online-suggestions-liste'), 0);
    return `<div class="empty">
    <h3>Aucun chant trouvé dans la base</h3>
    <p class="muted">Recherche en ligne automatique de propositions (fonction enrichie « Rechercher un chant en ligne ») :</p>
    <div id="online-suggestions-liste" style="margin-top:14px;text-align:left;max-width:560px;margin-left:auto;margin-right:auto;">
      <p class="muted" style="text-align:center;">Recherche en cours…</p>
    </div>
    <div class="row" style="justify-content:center;gap:10px;margin-top:14px;">
      <a class="btn" target="_blank" href="https://www.youtube.com/results?search_query=${encodeURIComponent(q+' chant liturgique catholique')}">Chercher une vidéo sur YouTube ↗</a>
      <a class="btn" target="_blank" href="https://www.google.com/search?q=${encodeURIComponent(q+' partition chant liturgique')}">Chercher une partition ↗</a>
    </div>
  </div>`;
  }
  return `
  <div class="card">
    <table>
      <tbody>
        ${sorted.map(c => `<tr><td><a onclick="nav('#/chants/${c.id}')" style="cursor:pointer;font-family:'Fraunces',serif;font-size:15px;">${esc(c.titre)}</a></td>
          <td style="text-align:right;"><span class="badge wine">${typeChantLabel(c.type_chant)}</span></td></tr>`).join('')}
      </tbody>
    </table>
  </div>
  ${q ? onlineSearchLinksBlock(q, 'Chercher un autre chant en ligne') : ''}`;
}
function runSearchChantsListe() {
  window._listeTitre = document.getElementById('lf-titre').value.trim();
  window._listeRef = document.getElementById('lf-ref').value.trim();
  render();
}
function clearSearchChantsListe() {
  window._listeTitre = ''; window._listeRef = '';
  render();
}

/* ---------------- Chants : recherche ---------------- */
function pageChants() {
  const hasSearched = !!window._searchDone;
  return `
  <div class="page-head">
    <div>
      <div class="crumb">Répertoire</div>
      <h1>Rechercher un chant</h1>
      <p class="muted">Recherche dans la base par titre ou référence. Si rien n’est trouvé, une recherche en ligne est proposée.</p>
    </div>
    ${can('edit') ? `<button class="primary" onclick="nav('#/chant-nouveau')">+ Ajouter un chant</button>` : ''}
  </div>
  <div class="card" style="max-width:640px;margin-bottom:18px;">
    <div class="grid2">
      <div class="field"><label>Titre du chant</label><input id="sf-titre" maxlength="100" value="${esc(window._searchTitre||'')}" placeholder="Ex. Sanctus"></div>
      <div class="field"><label>Référence du chant</label><input id="sf-ref" value="${esc(window._searchRef||'')}" placeholder="Ex. PS_27"></div>
    </div>
    <p class="muted" style="margin-top:-6px;">Au moins un des deux champs doit être renseigné.</p>
    <button class="primary" onclick="runSearchChant()">Soumettre</button>
  </div>
  <div id="chant-list-zone">${hasSearched ? renderChantList(searchChants()) : ''}</div>
  <div class="card" style="max-width:640px;margin-top:18px;">
    <h3>Rechercher une partition en ligne</h3>
    <p class="muted" style="margin-top:-6px;">Saisissez le titre ou la référence du chant : la recherche s’ouvre dans un nouvel onglet. Copiez ensuite le lien trouvé dans la fiche du chant (champ Partition).</p>
    <div class="grid2">
      <div class="field"><label>Titre du chant</label><input id="pf-search-titre" placeholder="Ex. Sanctus Liberia"></div>
      <div class="field"><label>Référence du chant</label><input id="pf-search-ref" placeholder="Ex. PS_27"></div>
    </div>
    <button class="primary" onclick="searchPartitionOnline()">Rechercher</button>
  </div>
  `;
}
function searchPartitionOnline(titreId, refId) {
  const titre = document.getElementById(titreId || 'pf-search-titre').value.trim();
  const ref = document.getElementById(refId || 'pf-search-ref').value.trim();
  if (!titre && !ref) { toast('Saisissez le titre ou la référence du chant'); return; }
  const q = [titre, ref, 'partition chant liturgique pdf'].filter(Boolean).join(' ');
  window.open('https://www.google.com/search?q=' + encodeURIComponent(q), '_blank');
}
function searchYoutubeOnline(titreId, refId) {
  const titre = document.getElementById(titreId).value.trim();
  const ref = document.getElementById(refId).value.trim();
  if (!titre && !ref) { toast('Saisissez le titre ou la référence du chant'); return; }
  const q = [titre, ref].filter(Boolean).join(' ');
  window.open('https://www.youtube.com/results?search_query=' + encodeURIComponent(q), '_blank');
}
function searchChants() {
  const titre = window._searchTitre || '';
  const ref = window._searchRef || '';
  return DB.chants.filter(c => {
    let ok = true;
    if (titre) ok = ok && fuzzyMatch(c.titre, titre);
    if (ref) ok = ok && fuzzyMatch([c.reference1, c.reference2].filter(Boolean).join(' '), ref);
    return ok;
  });
}
function runSearchChant() {
  const titre = document.getElementById('sf-titre').value.trim();
  const ref = document.getElementById('sf-ref').value.trim();
  if (!titre && !ref) { toast('Renseignez au moins le titre ou la référence'); return; }
  window._searchTitre = titre; window._searchRef = ref; window._searchDone = true;
  document.getElementById('chant-list-zone').innerHTML = renderChantList(searchChants());
}
const _onlineSearchToken = {};
async function fetchOnlineSuggestions(query, containerId) {
  containerId = containerId || 'online-suggestions';
  const zone = document.getElementById(containerId);
  if (!zone || !query) { if (zone) zone.innerHTML = ''; return; }
  const myToken = (_onlineSearchToken[containerId] = (_onlineSearchToken[containerId] || 0) + 1);
  try {
    const url = 'https://itunes.apple.com/search?term=' + encodeURIComponent(query + ' chant liturgique') + '&media=music&limit=5';
    const res = await fetch(url);
    if (!res.ok) throw new Error('HTTP ' + res.status);
    const data = await res.json();
    if (_onlineSearchToken[containerId] !== myToken) return; // une recherche plus récente a été lancée entre-temps
    const zoneNow = document.getElementById(containerId);
    if (!zoneNow) return; // l’utilisateur a changé de page entre-temps
    if (!data.results || !data.results.length) {
      zoneNow.innerHTML = `<p class="muted" style="text-align:center;">Aucune proposition trouvée en ligne pour « ${esc(query)} ».</p>`;
      return;
    }
    zoneNow.innerHTML = `<p class="muted" style="margin-bottom:8px;">Propositions trouvées en ligne :</p>` + data.results.map(r => `
      <div class="chant-row" style="cursor:default;">
        ${r.artworkUrl60 ? `<img src="${esc(r.artworkUrl60)}" style="width:44px;height:44px;border-radius:6px;flex-shrink:0;">` : '<div class="rib"></div>'}
        <div style="flex:1;">
          <div class="chant-title" style="font-size:14.5px;">${esc(r.trackName)}</div>
          <div class="chant-meta">${esc(r.artistName)}</div>
          ${r.previewUrl ? `<audio controls preload="none" style="width:100%;height:30px;margin-top:6px;"><source src="${esc(r.previewUrl)}"></audio>` : ''}
        </div>
        ${r.trackViewUrl ? `<a class="btn" target="_blank" href="${esc(r.trackViewUrl)}">Voir ↗</a>` : ''}
      </div>`).join('');
  } catch (e) {
    if (_onlineSearchToken[containerId] !== myToken) return;
    const zoneNow = document.getElementById(containerId);
    if (zoneNow) zoneNow.innerHTML = `<p class="muted" style="text-align:center;">Recherche en ligne indisponible pour le moment (pas de connexion, ou navigateur bloquant l’accès depuis un fichier local). Utilisez les liens ci-dessous.</p>`;
  }
}
function renderChantList(list) {
  const q = [window._searchTitre, window._searchRef].filter(Boolean).join(' ');
  if (!list.length) {
    setTimeout(() => fetchOnlineSuggestions(q), 0);
    return `<div class="empty">
    <h3>Aucun chant trouvé dans la base</h3>
    <p class="muted">Recherche en ligne automatique de propositions (fonction enrichie « Rechercher un chant en ligne ») :</p>
    <div id="online-suggestions" style="margin-top:14px;text-align:left;max-width:560px;margin-left:auto;margin-right:auto;">
      <p class="muted" style="text-align:center;">Recherche en cours…</p>
    </div>
    <div class="row" style="justify-content:center;gap:10px;margin-top:14px;">
      <a class="btn" target="_blank" href="https://www.youtube.com/results?search_query=${encodeURIComponent(q+' chant liturgique catholique')}">Chercher une vidéo sur YouTube ↗</a>
      <a class="btn" target="_blank" href="https://www.google.com/search?q=${encodeURIComponent(q+' partition chant liturgique')}">Chercher une partition ↗</a>
    </div>
  </div>`;
  }
  return `<div class="chant-list">${list.map(c => `
    <div class="chant-row" onclick="nav('#/chants/${c.id}')">
      <div class="rib"></div>
      <div style="flex:1;">
        <div class="chant-title">${esc(c.titre)}</div>
        <div class="chant-meta">${c.reference1 ? esc(c.reference1)+' · ' : ''}${c.compositeur ? esc(c.compositeur) : 'Compositeur non renseigné'}</div>
      </div>
      <span class="badge wine">${typeChantLabel(c.type_chant)}</span>
      <span class="badge gray">${typeMesseLabel(c.type_messe)}</span>
    </div>`).join('')}</div>
  ${q ? onlineSearchLinksBlock(q, 'Chercher un autre chant en ligne') : ''}`;
}

/* ---------------- Chant : fiche ---------------- */
function pageChantDetail(id) {
  const c = DB.chants.find(x => x.id == id);
  if (!c) return `<div class="empty">Chant introuvable. <a onclick="nav('#/chants-liste')">Retour</a></div>`;
  return `
  <div class="page-head">
    <div>
      <div class="crumb"><a onclick="nav('#/chants-liste')" style="cursor:pointer;">Chants</a> / Fiche</div>
      <h1>${esc(c.titre)}</h1>
      <div class="row" style="margin-top:6px;gap:8px;">
        <span class="badge wine">${typeChantLabel(c.type_chant)}</span>
        <span class="badge gray">${typeMesseLabel(c.type_messe)}</span>
        ${c.reference1 ? `<span class="stamp">${esc(c.reference1)}</span>` : ''}
        ${c.reference2 ? `<span class="stamp">${esc(c.reference2)}</span>` : ''}
      </div>
    </div>
    <div class="row no-print">
      <button onclick="window.print()">Imprimer</button>
      ${can('edit') ? `<button onclick="nav('#/chant-edit/${c.id}')">Modifier</button>` : ''}
      ${can('edit') ? `<button class="danger" onclick="deleteChant(${c.id})">Supprimer</button>` : ''}
    </div>
  </div>
  <div class="grid2">
    <div class="card">
      <h3>Paroles</h3>
      ${c.texte ? `<div class="lyrics">${esc(c.texte).replace(/(\d\. )/g,'<br><br>$1').replace(/^<br><br>/,'')}</div>` : `<p class="muted">Texte non renseigné dans la base.</p>`}
    </div>
    <div>
      <div class="card" style="margin-bottom:16px;">
        <h3>Informations</h3>
        <table>
          <tr><td class="muted">Compositeur</td><td>${esc(c.compositeur) || '—'}</td></tr>
          <tr><td class="muted">Harmonisation</td><td>${esc(c.harmonisation) || '—'}</td></tr>
          <tr><td class="muted">Référence 1</td><td>${esc(c.reference1) || '—'}</td></tr>
          <tr><td class="muted">Référence 2</td><td>${esc(c.reference2) || '—'}</td></tr>
          ${((DB.schema && DB.schema.chants) || []).map(f => `<tr><td class="muted">${esc(f.label)}</td><td>${esc(c.custom && c.custom[f.key]) || '—'}</td></tr>`).join('')}
        </table>
      </div>
      <div class="card">
        <h3>YouTube</h3>
        ${c.youtube ? `<p>▶ <a href="${esc(c.youtube)}" target="_blank">Voir la vidéo sur YouTube ↗</a></p>` : `<p class="muted">Aucun lien YouTube renseigné.</p>`}
        <h3 style="margin-top:16px;">Audio (voix décomposées)</h3>
        ${renderAudios(c.audios)}
        <h3 style="margin-top:16px;">Partition</h3>
        ${renderPartition(c.partition)}
      </div>
      ${can('edit') && !c.partition ? `
      <div class="card no-print" style="margin-top:16px;">
        <h3>Rechercher une partition en ligne</h3>
        <p class="muted" style="margin-top:-6px;">La recherche s’ouvre dans un nouvel onglet. Copiez ensuite le lien trouvé via « Modifier » (champ Partition).</p>
        <div class="grid2">
          <div class="field"><label>Titre du chant</label><input id="cdp-titre" value="${esc(c.titre)}"></div>
          <div class="field"><label>Référence du chant</label><input id="cdp-ref" value="${esc(c.reference1)}"></div>
        </div>
        <button class="primary" onclick="searchPartitionOnline('cdp-titre','cdp-ref')">Rechercher</button>
      </div>` : ''}
      ${can('edit') && !c.youtube ? `
      <div class="card no-print" style="margin-top:16px;">
        <h3>Rechercher sur YouTube</h3>
        <p class="muted" style="margin-top:-6px;">La recherche s’ouvre dans un nouvel onglet. Copiez ensuite le lien trouvé via « Modifier » (champ Lien YouTube).</p>
        <div class="grid2">
          <div class="field"><label>Titre du chant</label><input id="cdy-titre" value="${esc(c.titre)}"></div>
          <div class="field"><label>Référence du chant</label><input id="cdy-ref" value="${esc(c.reference1)}"></div>
        </div>
        <button class="primary" onclick="searchYoutubeOnline('cdy-titre','cdy-ref')">Rechercher</button>
      </div>` : ''}
    </div>
  </div>`;
}
async function deleteChant(id) {
  if (!confirm('Supprimer ce chant du répertoire ?')) return;
  const { error } = await sb.from('chants').delete().eq('id', id);
  if (error) { toast('Erreur lors de la suppression : ' + error.message); return; }
  DB.chants = DB.chants.filter(c => c.id !== id);
  toast('Chant supprimé'); nav('#/chants-liste'); render();
}

/* ---------------- Chant : formulaire ---------------- */
function renderCustomFieldInputs(entity, record) {
  const fields = (DB.schema && DB.schema[entity]) || [];
  if (!fields.length) return '';
  return fields.map(f => {
    const val = (record.custom && record.custom[f.key]) || '';
    const type = f.type === 'number' ? 'number' : (f.type === 'date' ? 'date' : 'text');
    return `<div class="field"><label>${esc(f.label)} <span class="badge gold" style="font-size:9px;">perso.</span></label><input id="custom-${f.key}" type="${type}" value="${esc(val)}"></div>`;
  }).join('');
}
function readCustomFieldValues(entity) {
  const fields = (DB.schema && DB.schema[entity]) || [];
  const out = {};
  fields.forEach(f => {
    const el = document.getElementById('custom-' + f.key);
    if (el) out[f.key] = el.value.trim() || null;
  });
  return out;
}
function pageChantForm(id) {
  const editing = !!id;
  const c = editing ? DB.chants.find(x => x.id == id) : { titre:'', reference1:'', reference2:'', texte:'', type_messe:'', type_chant:'', compositeur:'', harmonisation:'', partition:'C:\\Liturgie\\Medias\\Partitions\\', audios:[] };
  if (editing && !c) return `<div class="empty">Chant introuvable.</div>`;
  return `
  <div class="page-head">
    <div>
      <div class="crumb"><a onclick="nav('#/chants-liste')" style="cursor:pointer;">Chants</a> / ${editing ? 'Modifier' : 'Nouveau'}</div>
      <h1>${editing ? 'Modifier le chant' : 'Ajouter un chant'}</h1>
    </div>
  </div>
  <div class="card" style="max-width:720px;">
    <div class="grid2">
      <div class="field"><label>Titre *</label><input id="f-titre" value="${esc(c.titre)}"></div>
      <div class="field"><label>Compositeur</label><input id="f-compositeur" value="${esc(c.compositeur)}"></div>
      <div class="field"><label>Référence 1</label><input id="f-ref1" value="${esc(c.reference1)}"></div>
      <div class="field"><label>Référence 2</label><input id="f-ref2" value="${esc(c.reference2)}"></div>
      <div class="field"><label>Type de chant</label>
        <select id="f-typechant"><option value="">—</option>${Object.entries(DB.type_chant).map(([k,v])=>`<option value="${k}" ${c.type_chant===k?'selected':''}>${v}</option>`).join('')}</select>
      </div>
      <div class="field"><label>Catégorie <span class="muted">(temps liturgique ou occasion)</span></label>
        <select id="f-typemesse"><option value="">—</option>${Object.entries(DB.type_messe).map(([k,v])=>`<option value="${k}" ${c.type_messe===k?'selected':''}>${v}</option>`).join('')}</select>
      </div>
      <div class="field"><label>Harmonisation</label><input id="f-harmo" value="${esc(c.harmonisation)}"></div>
      <div class="field" style="grid-column:1/-1;"><label>Lien YouTube</label><input id="f-youtube" value="${esc(c.youtube)}" placeholder="https://youtu.be/…"></div>
      <div class="field" style="grid-column:1/-1;">
        <label>Pistes audio décomposées <span class="muted">(une par ligne : Nom de la piste | chemin ou URL — ex. les 4 voix)</span></label>
        <textarea id="f-audios" rows="3" placeholder="Refrain|media/Audios/mon-chant-ref.aac">${esc((c.audios||[]).map(a=>`${a.label}|${a.src||''}`).join('\n'))}</textarea>
      </div>
      <div class="field" style="grid-column:1/-1;"><label>Lien / chemin partition <span class="muted">(complétez avec le nom du fichier .pdf, ou collez une URL)</span></label><input id="f-partition" value="${esc(c.partition)}"></div>
      <div class="field" style="grid-column:1/-1;"><label>Texte</label><textarea id="f-texte" rows="8">${esc(c.texte)}</textarea></div>
      ${renderCustomFieldInputs('chants', c)}
    </div>
    <div class="row">
      <button class="primary" onclick="saveChant(${editing ? c.id : 'null'})">Enregistrer</button>
      <button class="ghost" onclick="goBack('#/chants-liste')">Annuler</button>
    </div>
  </div>`;
}
async function saveChant(id) {
  const val = (i) => document.getElementById(i).value.trim();
  const titre = val('f-titre');
  if (!titre) { toast('Le titre est obligatoire'); return; }
  const audios = document.getElementById('f-audios').value.split('\n')
    .map(l => l.trim()).filter(Boolean)
    .map(l => { const [label, ...rest] = l.split('|'); return { label: (label||'Piste').trim(), src: rest.join('|').trim() || null }; });
  let partitionVal = val('f-partition');
  if (/^C:\\Liturgie\\Medias\\Partitions\\?$/i.test(partitionVal)) partitionVal = '';
  const data = {
    titre, reference1: val('f-ref1')||null, reference2: val('f-ref2')||null,
    texte: val('f-texte')||null, type_messe: val('f-typemesse')||null, type_chant: val('f-typechant')||null,
    compositeur: val('f-compositeur')||null, harmonisation: val('f-harmo')||null,
    partition: partitionVal||null, youtube: val('f-youtube')||null, audios,
    custom: readCustomFieldValues('chants'),
  };
  let finalId = id;
  if (id) {
    const { error } = await sb.from('chants').update(data).eq('id', id);
    if (error) { toast('Erreur lors de l’enregistrement : ' + error.message); return; }
    const idx = DB.chants.findIndex(c => c.id === id);
    DB.chants[idx] = { ...DB.chants[idx], ...data };
  } else {
    const { data: inserted, error } = await sb.from('chants').insert(data).select().single();
    if (error) { toast('Erreur lors de l’enregistrement : ' + error.message); return; }
    finalId = inserted.id;
    DB.chants.push(inserted);
  }
  window._searchDone = false; window._searchTitre = ''; window._searchRef = '';
  window._listeTitre = ''; window._listeRef = '';
  toast('Chant enregistré'); nav('#/chants/' + finalId); render();
}
/* ---------------- Programmes : liste ---------------- */
function pageProgrammes() {
  const list = [...DB.programmes].sort((a,b)=>a.date.localeCompare(b.date));
  return `
  <div class="page-head">
    <div>
      <div class="crumb">Préparation</div>
      <h1>Afficher les programmes</h1>
      <p class="muted">Programmes de chant générés pour les offices.</p>
    </div>
    ${can('edit') ? `<button class="primary" onclick="nav('#/programme-nouveau')">+ Générer un programme</button>` : ''}
  </div>
  ${list.length ? `<div class="chant-list">${list.map(p => `
    <div class="chant-row" onclick="nav('#/programmes/${p.id}')">
      <div class="rib" style="background:${p.statut==='validé' ? 'var(--sage)' : 'var(--gold)'}"></div>
      <div style="flex:1;">
        <div class="chant-title">${esc(p.titre)}</div>
        <div class="chant-meta">${fmtDate(p.date)} · ${clocherLabel(p.lieu)} · ${typeMesseLabel(p.type_messe)}</div>
      </div>
      <span class="badge ${p.statut==='validé' ? 'sage' : 'gray'}">${p.statut}</span>
    </div>`).join('')}</div>` : `<div class="empty"><h3>Aucun programme</h3><p class="muted">Commencez par en générer un.</p></div>`}
  `;
}

/* ---------------- Programmes : génération ---------------- */
function defaultProgTitre(date, typeMesse, lieu) {
  const tmLabel = typeMesse ? typeMesseLabel(typeMesse) : '';
  const lieuText = lieu || 'Paroisse';
  const dateText = date ? fmtDate(date) : '';
  return `Célébration${tmLabel ? ' ' + tmLabel : ''} à ${lieuText} le ${dateText} à 11h`;
}
function updateProgTitrePlaceholder() {
  const dateEl = document.getElementById('pf-date'), tmEl = document.getElementById('pf-typemesse'), lieuEl = document.getElementById('pf-lieu'), titreEl = document.getElementById('pf-titre');
  if (!dateEl || !titreEl) return;
  titreEl.placeholder = defaultProgTitre(dateEl.value, tmEl.value, lieuEl.value ? clocherLabel(lieuEl.value) : '');
}
function pageProgrammeForm() {
  if (!can('edit')) return pageForbidden();
  const prefillDate = window._prefillProgDate || todayISO();
  const prefillLieu = window._prefillProgLieu || Object.keys(DB.clochers)[0];
  window._prefillProgDate = null; window._prefillProgLieu = null;
  setTimeout(checkDateLieuConflict, 0);
  return `
  <div class="page-head">
    <div>
      <div class="crumb"><a onclick="nav('#/programmes')" style="cursor:pointer;">Programmes</a> / Nouveau</div>
      <h1>Générer un programme</h1>
      <p class="muted">L’outil compare le texte de la lecture du jour aux chants du répertoire et propose le chant le plus adapté à chaque emplacement.</p>
    </div>
  </div>
  <div class="card" style="max-width:760px;margin-bottom:18px;">
    <div class="field">
      <label>Dupliquer un programme existant <span class="muted">(optionnel — copie les chants ci-dessous)</span></label>
      <select id="pf-duplicate" onchange="duplicateProgramme()">
        <option value="">— Nouveau programme vierge —</option>
        ${[...DB.programmes].sort((a,b)=>b.date.localeCompare(a.date)).map(p => `<option value="${p.id}">${fmtDate(p.date)} — ${esc(p.titre)}</option>`).join('')}
      </select>
    </div>
    <div class="grid3">
      <div class="field"><label>Date de la messe *</label><input type="date" id="pf-date" value="${prefillDate}" oninput="updateProgTitrePlaceholder();checkDateLieuConflict()"></div>
      <div class="field"><label>Type de messe</label>
        <select id="pf-typemesse" onchange="updateProgTitrePlaceholder()"><option value="">—</option>${Object.entries(DB.type_messe).map(([k,v])=>`<option value="${k}">${v}</option>`).join('')}</select>
      </div>
      <div class="field"><label>Ordinaire de la messe</label>
        <select id="pf-ordinaire"><option value="">—</option>${Object.entries(DB.ordinaire_messe).map(([k,v])=>`<option value="${k}">${v}</option>`).join('')}</select>
      </div>
    </div>
    <div class="field"><label>Titre du programme</label><input id="pf-titre" placeholder="${esc(defaultProgTitre(prefillDate, '', clocherLabel(prefillLieu)))}"></div>
    <div class="field"><label>Lieu (clocher)</label>
      <select id="pf-lieu" onchange="updateProgTitrePlaceholder();checkDateLieuConflict()">${Object.entries(DB.clochers).map(([k,v])=>`<option value="${k}" ${String(prefillLieu)===String(k)?'selected':''}>${v}</option>`).join('')}</select>
    </div>
    <div id="pf-date-warning"></div>
    <div class="field">
      <label>Texte de la lecture / évangile du jour <span class="muted">(collé manuellement — l’outil se connecterait normalement à evangeli.net)</span></label>
      <textarea id="pf-lecture" rows="5" placeholder="Collez ici le texte de la lecture ou de l’évangile du jour pour affiner la proposition de chants…"></textarea>
    </div>
    <button class="primary" id="pf-generate-btn" onclick="generateProgramme()">Proposer les chants</button>
  </div>
  <div id="prog-gen-result"></div>
  `;
}
function checkDateLieuConflict() {
  const dateEl = document.getElementById('pf-date'), lieuEl = document.getElementById('pf-lieu');
  const warnEl = document.getElementById('pf-date-warning'), btn = document.getElementById('pf-generate-btn');
  if (!dateEl || !lieuEl || !warnEl) return;
  const date = dateEl.value, lieu = lieuEl.value;
  const dupId = Number(document.getElementById('pf-duplicate') ? document.getElementById('pf-duplicate').value : 0);
  const conflict = DB.programmes.find(p => p.date === date && String(p.lieu||'') === String(lieu||'') && p.id !== dupId);
  if (conflict) {
    warnEl.innerHTML = `<p style="color:#8a2b2b;font-size:12.5px;margin:6px 0 0;">⚠ Un programme existe déjà à cette date pour ${esc(clocherLabel(lieu))} : « ${esc(conflict.titre)} ». Choisissez une autre date ou un autre lieu.</p>`;
    if (btn) btn.disabled = true;
  } else {
    warnEl.innerHTML = '';
    if (btn) btn.disabled = false;
  }
}

function duplicateProgramme() {
  const id = Number(document.getElementById('pf-duplicate').value);
  if (!id) { window._duplicateSourceDate = null; document.getElementById('prog-gen-result').innerHTML = ''; checkDateLieuConflict(); return; }
  const src = DB.programmes.find(p => p.id === id);
  if (!src) return;
  window._duplicateSourceDate = src.date;
  window._genSlots = { ...src.slots };
  document.getElementById('pf-typemesse').value = src.type_messe || '';
  document.getElementById('pf-ordinaire').value = src.ordinaire_messe || '';
  if (src.lieu) document.getElementById('pf-lieu').value = src.lieu;
  document.getElementById('pf-lecture').value = src.lecture || '';
  updateProgTitrePlaceholder();
  checkDateLieuConflict();
  renderDuplicatePreview(src);
  toast('Programme dupliqué — choisissez une nouvelle date puis enregistrez');
}
function renderDuplicatePreview(src) {
  const ordinaire = src.ordinaire_messe;
  document.getElementById('prog-gen-result').innerHTML = `
    <div class="card">
      <h3>Aperçu dupliqué depuis « ${esc(src.titre)} »</h3>
      <p class="muted" style="margin-top:-6px;">Chants copiés depuis le programme du ${fmtDate(src.date)}. Choisissez une nouvelle date ci-dessus, ajustez si besoin, puis enregistrez.</p>
      ${SLOTS.map(slot => {
        const val = src.slots[slot.code];
        if (val === 'ORDINAIRE') {
          return `<div class="prog-slot">
            <label>${slot.label}</label>
            <div><span class="badge gold">Ordinaire</span> ${esc(ordinaire ? DB.ordinaire_messe[ordinaire] : '')}</div>
            <span></span>
          </div>`;
        }
        const isMention = DEFAULT_MENTIONS[slot.code] && (val === 'MENTION' || !val);
        const candidates = DB.chants.filter(c => c.type_chant === slot.code);
        const currentMismatch = (val && val !== 'MENTION' && !candidates.find(c => String(c.id) === String(val))) ? DB.chants.find(c => String(c.id) === String(val)) : null;
        return `<div class="prog-slot">
          <label>${slot.label}</label>
          <select onchange="window._genSlots['${slot.code}']=this.value||null">
            ${DEFAULT_MENTIONS[slot.code] ? `<option value="MENTION" ${isMention?'selected':''}>${esc(DEFAULT_MENTIONS[slot.code])}</option>` : ''}
            <option value="" ${!DEFAULT_MENTIONS[slot.code] && !val ? 'selected' : ''}>— aucun${DEFAULT_MENTIONS[slot.code]?'':' chant'} —</option>
            ${candidates.map(c => `<option value="${c.id}" ${String(val)===String(c.id)?'selected':''}>${esc(c.titre)}</option>`).join('')}
            ${currentMismatch ? `<option value="${currentMismatch.id}" selected>${esc(currentMismatch.titre)}</option>` : ''}
          </select>
          <span></span>
        </div>`;
      }).join('')}
      <div class="row" style="margin-top:16px;">
        <button class="primary" onclick="saveProgramme(document.getElementById('pf-date').value, document.getElementById('pf-typemesse').value)">Enregistrer le programme</button>
      </div>
    </div>`;
}
function generateProgramme() {
  window._duplicateSourceDate = null;
  const dupSel = document.getElementById('pf-duplicate');
  if (dupSel) dupSel.value = '';
  checkDateLieuConflict();
  const date = document.getElementById('pf-date').value;
  const lieuCheck = document.getElementById('pf-lieu').value;
  if (DB.programmes.some(p => p.date === date && String(p.lieu||'') === String(lieuCheck||''))) {
    toast('Un programme existe déjà à cette date pour ce lieu !');
    return;
  }
  const typeMesse = document.getElementById('pf-typemesse').value;
  const ordinaire = document.getElementById('pf-ordinaire').value;
  const lecture = document.getElementById('pf-lecture').value.trim();
  const keywords = lecture ? extractKeywords(lecture) : [];
  window._genSlots = {};
  const rows = SLOTS.map(slot => {
    if (ordinaire && ORDINAIRE_SLOTS.includes(slot.code)) {
      window._genSlots[slot.code] = 'ORDINAIRE';
      return { slot, options: [], useOrdinaire: true };
    }
    let candidates = DB.chants.filter(c => c.type_chant === slot.code);
    if (typeMesse) {
      const matching = candidates.filter(c => !c.type_messe || c.type_messe === typeMesse);
      if (matching.length) candidates = matching;
    }
    candidates = candidates.map(c => ({ c, score: lecture ? scoreOverlap(c.texte || c.titre, lecture) : null }));
    if (lecture) candidates.sort((a,b) => b.score - a.score);
    const top2 = candidates.slice(0, 2);
    if (DEFAULT_MENTIONS[slot.code]) {
      window._genSlots[slot.code] = 'MENTION';
      return { slot, options: top2, useMention: true };
    }
    if (top2[0]) window._genSlots[slot.code] = top2[0].c.id;
    return { slot, options: top2 };
  });
  window._genRows = rows;
  document.getElementById('prog-gen-result').innerHTML = `
    <div class="card">
      <h3>Proposition de programme</h3>
      <p class="muted" style="margin-top:-6px;">Un chant a été présélectionné pour chaque temps où un candidat existe.${ordinaire ? ' Les temps couverts par l’ordinaire de la messe utilisent sa désignation.' : ' Vous pouvez changer chaque choix.'} Sans candidat en base, une recherche en ligne est proposée.</p>
      ${rows.map(({slot, options, useOrdinaire, useMention}) => {
        if (useOrdinaire) return `
        <div class="prog-slot">
          <label>${slot.label}</label>
          <div><span class="badge gold">Ordinaire</span> ${esc(DB.ordinaire_messe[ordinaire] || ordinaire)}</div>
          <span></span>
        </div>`;
        const containerId = `online-slot-${slot.code}`;
        const ytQuery = [...keywords, typeChantLabel(slot.code), 'chant catholique'].filter(Boolean).join(' ');
        const ytLink = `<a class="btn" style="padding:4px 9px;font-size:11.5px;" target="_blank" href="https://www.youtube.com/results?search_query=${encodeURIComponent(ytQuery)}">▶ YouTube</a>`;
        if (useMention) return `
        <div class="prog-slot">
          <label>${slot.label}</label>
          <div>
            <select id="gen-slot-${slot.code}" onchange="window._genSlots['${slot.code}']=this.value||null">
              <option value="MENTION" selected>${esc(DEFAULT_MENTIONS[slot.code])}</option>
              <option value="">— aucun —</option>
              ${options.map(o => `<option value="${o.c.id}">${esc(o.c.titre)}</option>`).join('')}
              ${DB.chants.filter(c => c.type_chant === slot.code && !options.find(o=>o.c.id===c.id)).map(c => `<option value="${c.id}">${esc(c.titre)}</option>`).join('')}
            </select>
            <div class="row" style="margin-top:6px;gap:6px;">
              <input id="new-gen-chant-${slot.code}" placeholder="Chant trouvé en ligne : nom du chant…" style="flex:1;font-size:12px;padding:6px 8px;">
              <button class="ghost" style="font-size:11px;padding:5px 9px;white-space:nowrap;" onclick="quickCreateChant('${slot.code}','gen-slot-${slot.code}','new-gen-chant-${slot.code}')">+ Ajouter au répertoire</button>
            </div>
          </div>
          <span></span>
        </div>`;
        if (!options.length) {
          setTimeout(() => fetchSlotSuggestions(ytQuery, containerId), 0);
        }
        return `
        <div class="prog-slot" style="align-items:flex-start;">
          <label style="padding-top:8px;">${slot.label}</label>
          <div>
            <select id="gen-slot-${slot.code}" onchange="window._genSlots['${slot.code}']=this.value||null">
              <option value="">— aucun chant —</option>
              ${options.map(o => `<option value="${o.c.id}" selected>${esc(o.c.titre)}</option>`).join('')}
              ${DB.chants.filter(c => c.type_chant === slot.code && !options.find(o=>o.c.id===c.id)).map(c => `<option value="${c.id}">${esc(c.titre)}</option>`).join('')}
            </select>
            ${options.length && options[0].score !== null ? `<span class="score">${options[0].score}% de correspondance</span>` : options.length ? `<span class="muted" style="font-size:11px;">Filtré par type</span>` : `<div id="${containerId}" style="margin-top:6px;"><p class="muted" style="font-size:12px;">Recherche en ligne de propositions…</p></div>`}
            <div class="row" style="margin-top:6px;gap:6px;">
              <input id="new-gen-chant-${slot.code}" placeholder="Chant trouvé en ligne : nom du chant…" style="flex:1;font-size:12px;padding:6px 8px;">
              <button class="ghost" style="font-size:11px;padding:5px 9px;white-space:nowrap;" onclick="quickCreateChant('${slot.code}','gen-slot-${slot.code}','new-gen-chant-${slot.code}')">+ Ajouter au répertoire</button>
            </div>
          </div>
          <div style="padding-top:6px;">${ytLink}</div>
        </div>`;
      }).join('')}
      <div class="row" style="margin-top:16px;">
        <button class="primary" onclick="saveProgramme('${date}','${typeMesse}')">Enregistrer le programme</button>
      </div>
    </div>`;
}

function saveProgramme(date, typeMesse) {
  if (window._duplicateSourceDate && date === window._duplicateSourceDate) {
    toast('Un programme existe déjà à cette date !');
    return;
  }
  const lieu = document.getElementById('pf-lieu').value;
  if (DB.programmes.some(p => p.date === date && String(p.lieu||'') === String(lieu||''))) {
    toast('Un programme existe déjà à cette date pour ce lieu !');
    return;
  }
  const titre = document.getElementById('pf-titre').value.trim() || defaultProgTitre(date, typeMesse, clocherLabel(lieu));
  const ordinaire = document.getElementById('pf-ordinaire').value;
  const lecture = document.getElementById('pf-lecture').value.trim();
  const prog = {
    id: DB.nextProgId++, titre, date, lieu, type_messe: typeMesse || null, ordinaire_messe: ordinaire || null,
    lecture: lecture || null, slots: { ...window._genSlots }, statut: 'brouillon',
  };
  DB.programmes.push(prog);
  window._duplicateSourceDate = null;
  persist(); toast('Programme enregistré en brouillon'); nav('#/programmes/' + prog.id); render();
}

/* ---------------- Programmes : détail ---------------- */
const STOPWORDS_FR = new Set(['dans','avec','pour','vous','nous','leur','leurs','cette','ces','les','des','une','sont','qui','que','quoi','comme','tout','tous','toute','toutes','mais','donc','car','ainsi','plus','moins','sans','sous','vers','entre','alors','lorsque','quand','être','avoir','fait','faire','dieu','seigneur','jesus','christ','jesus-christ']);
function extractKeywords(text, n) {
  n = n || 8;
  const words = normStr(text).split(' ').filter(w => w.length >= 4 && !STOPWORDS_FR.has(w));
  const freq = {};
  words.forEach(w => { freq[w] = (freq[w] || 0) + 1; });
  return Object.keys(freq).sort((a,b) => freq[b]-freq[a]).slice(0, n);
}
async function fetchSlotSuggestions(query, containerId) {
  const zone = document.getElementById(containerId);
  if (!zone || !query) return;
  const myToken = (_onlineSearchToken[containerId] = (_onlineSearchToken[containerId] || 0) + 1);
  try {
    const url = 'https://itunes.apple.com/search?term=' + encodeURIComponent(query) + '&media=music&limit=3';
    const res = await fetch(url);
    if (!res.ok) throw new Error('HTTP ' + res.status);
    const data = await res.json();
    if (_onlineSearchToken[containerId] !== myToken) return;
    const zoneNow = document.getElementById(containerId);
    if (!zoneNow) return;
    if (!data.results || !data.results.length) {
      zoneNow.innerHTML = `<p class="muted" style="font-size:12px;">Aucune proposition en ligne pour « ${esc(query)} ».</p>`;
      return;
    }
    zoneNow.innerHTML = data.results.map(r => `
      <div class="row" style="gap:8px;padding:5px 0;">
        ${r.artworkUrl60 ? `<img src="${esc(r.artworkUrl60)}" style="width:30px;height:30px;border-radius:5px;flex-shrink:0;">` : ''}
        <div style="flex:1;min-width:0;">
          <div style="font-size:12.5px;font-weight:500;">${esc(r.trackName)}</div>
          <div class="muted" style="font-size:11px;">${esc(r.artistName)}</div>
        </div>
        ${r.trackViewUrl ? `<a class="btn" style="padding:4px 9px;font-size:11.5px;" target="_blank" href="${esc(r.trackViewUrl)}">Voir ↗</a>` : ''}
      </div>`).join('');
  } catch (e) {
    if (_onlineSearchToken[containerId] !== myToken) return;
    const zoneNow = document.getElementById(containerId);
    if (zoneNow) zoneNow.innerHTML = `<p class="muted" style="font-size:12px;">Recherche en ligne indisponible (hors connexion).</p>`;
  }
}
function pageProgrammeDetail(id) {
  const p = DB.programmes.find(x => x.id == id);
  if (!p) return `<div class="empty">Programme introuvable. <a onclick="nav('#/programmes')">Retour</a></div>`;
  const editable = can('edit');
  const keywords = p.lecture ? extractKeywords(p.lecture) : [];
  return `
  <div class="page-head">
    <div>
      <div class="crumb"><a onclick="nav('#/programmes')" style="cursor:pointer;">Programmes</a> / Détail</div>
      <h1>${esc(p.titre)}</h1>
      <div class="row" style="margin-top:6px;gap:8px;">
        <span class="badge ${p.statut==='validé' ? 'sage' : 'gray'}">${p.statut}</span>
        <span class="stamp">${fmtDate(p.date)}</span>
        <span class="stamp">${clocherLabel(p.lieu)}</span>
        ${p.type_messe ? `<span class="badge wine">${typeMesseLabel(p.type_messe)}</span>` : ''}
      </div>
    </div>
    <div class="row no-print">
      <button onclick="window.print()">Imprimer</button>
      ${can('validate') && p.statut !== 'validé' ? `<button class="primary" onclick="validateProgramme(${p.id})">Valider</button>` : ''}
      ${can('edit') ? `<button onclick="sendNotification(${p.id})">Notifier les utilisateurs</button>` : ''}
      ${can('edit') ? `<button class="danger" onclick="deleteProgramme(${p.id})">Supprimer</button>` : ''}
    </div>
  </div>
  ${(() => {
    const incomplete = programmeIncompleteChants(p);
    if (!incomplete.length) return '';
    return `<div class="card" style="margin-bottom:16px;border-color:#d8a94a;background:var(--gold-tint);">
      <h3 style="font-size:14.5px;">⚠ Programme non validable — ${incomplete.length} chant(s) incomplet(s)</h3>
      <ul style="margin:6px 0 0;padding-left:18px;font-size:13px;">
        ${incomplete.map(i => `<li><b>${esc(i.slot)}</b> : ${esc(i.titre)} — ${esc(i.reason)}</li>`).join('')}
      </ul>
      <p class="muted" style="margin:8px 0 0;font-size:12px;">Complétez la partition et l’audio de chaque chant concerné (page Chants) pour pouvoir valider ce programme.</p>
    </div>`;
  })()}
  <div class="card">
    ${editable ? `<p class="muted" style="margin-top:-6px;">Modifiez chaque chant ci-dessous, ou saisissez un chant trouvé en ligne pour l’ajouter au répertoire, puis enregistrez.</p>` : ''}
    ${SLOTS.map(slot => {
      const val = p.slots[slot.code];
      if (val === 'ORDINAIRE') {
        const label = p.ordinaire_messe ? DB.ordinaire_messe[p.ordinaire_messe] : null;
        return `<div class="prog-slot">
          <label>${slot.label}</label>
          <div>${label ? `<span class="badge gold">Ordinaire</span> ${esc(label)}` : `<span class="muted">— ordinaire non renseigné —</span>`}</div>
          <span></span>
        </div>`;
      }
      const isMention = DEFAULT_MENTIONS[slot.code] && (val === 'MENTION' || !val);
      const ytQuery = [...keywords, typeChantLabel(slot.code), 'chant catholique'].filter(Boolean).join(' ');
      const ytLink = `<a class="btn" style="padding:4px 9px;font-size:11.5px;" target="_blank" href="https://www.youtube.com/results?search_query=${encodeURIComponent(ytQuery)}">▶ YouTube</a>`;
      const c = (val && val !== 'MENTION') ? DB.chants.find(x => x.id == val) : null;
      if (!editable) {
        const warn = c && !isChantComplete(c) ? `<span class="badge gold" style="margin-left:6px;">⚠ incomplet</span>` : '';
        return `<div class="prog-slot">
          <label>${slot.label}</label>
          <div>${c ? `<a onclick="nav('#/chants/${c.id}')" style="cursor:pointer;">${esc(c.titre)}</a>${warn}` : isMention ? `<span class="badge sage">${esc(DEFAULT_MENTIONS[slot.code])}</span>` : `<span class="muted">— non renseigné —</span>`}</div>
          ${ytLink}
        </div>`;
      }
      const candidates = DB.chants.filter(ch => ch.type_chant === slot.code);
      const currentMismatch = (c && !candidates.find(ch => String(ch.id) === String(val))) ? c : null;
      const noLocalCandidate = !candidates.length;
      const containerId = `online-slot-${slot.code}`;
      if (noLocalCandidate && !val && !DEFAULT_MENTIONS[slot.code]) {
        setTimeout(() => fetchSlotSuggestions(ytQuery, containerId), 0);
      }
      return `<div class="prog-slot" style="align-items:flex-start;">
        <label style="padding-top:8px;">${slot.label}</label>
        <div>
          <select id="pd-slot-${slot.code}">
            ${DEFAULT_MENTIONS[slot.code] ? `<option value="MENTION" ${isMention?'selected':''}>${esc(DEFAULT_MENTIONS[slot.code])}</option>` : ''}
            <option value="" ${!DEFAULT_MENTIONS[slot.code] && !val ? 'selected' : ''}>— aucun${DEFAULT_MENTIONS[slot.code]?'':' chant'} —</option>
            ${candidates.map(ch => `<option value="${ch.id}" ${String(val)===String(ch.id)?'selected':''}>${esc(ch.titre)}</option>`).join('')}
            ${currentMismatch ? `<option value="${currentMismatch.id}" selected>${esc(currentMismatch.titre)}</option>` : ''}
          </select>
          ${c && !isChantComplete(c) ? `<div class="muted" style="font-size:11px;margin-top:3px;">⚠ partition ou audio manquant — <a onclick="nav('#/chant-edit/${c.id}')" style="cursor:pointer;">compléter</a></div>` : ''}
          ${noLocalCandidate && !val && !DEFAULT_MENTIONS[slot.code] ? `<div id="${containerId}" style="margin-top:6px;"><p class="muted" style="font-size:12px;">Recherche en ligne de propositions…</p></div>` : ''}
          <div class="row" style="margin-top:6px;gap:6px;">
            <input id="new-chant-${slot.code}" placeholder="Chant trouvé en ligne : nom du chant…" style="flex:1;font-size:12px;padding:6px 8px;">
            <button class="ghost" style="font-size:11px;padding:5px 9px;white-space:nowrap;" onclick="quickCreateChant('${slot.code}','pd-slot-${slot.code}','new-chant-${slot.code}')">+ Ajouter au répertoire</button>
          </div>
        </div>
        <div style="padding-top:6px;">${ytLink}</div>
      </div>`;
    }).join('')}
    ${editable ? `<div class="row" style="margin-top:16px;"><button class="primary" onclick="saveProgrammeSlots(${p.id})">Enregistrer les modifications</button></div>` : ''}
  </div>
  ${p.lecture ? `<div class="card" style="margin-top:16px;"><h3>Lecture du jour utilisée</h3><p style="white-space:pre-wrap;">${esc(p.lecture)}</p></div>` : ''}
  `;
}
async function quickCreateChant(slotCode, selectId, inputId) {
  if (!can('edit')) return;
  const input = document.getElementById(inputId);
  const titre = input.value.trim();
  if (!titre) { toast('Saisissez le nom du chant trouvé en ligne'); return; }
  const { data: inserted, error } = await sb.from('chants').insert({
    titre, type_chant: slotCode, reference1: null, reference2: null, texte: null,
    type_messe: null, compositeur: null, harmonisation: null, partition: null, youtube: null, audios: [], custom: {},
  }).select().single();
  if (error) { toast('Erreur lors de l’ajout : ' + error.message); return; }
  DB.chants.push(inserted);
  const newId = inserted.id;
  const select = document.getElementById(selectId);
  if (select) {
    const opt = document.createElement('option');
    opt.value = String(newId); opt.textContent = titre;
    select.appendChild(opt);
    select.value = String(newId);
    select.dispatchEvent(new Event('change'));
  }
  input.value = '';
  toast('Chant ajouté au répertoire — pensez à compléter partition et audio avant de valider');
}
function saveProgrammeSlots(id) {
  const p = DB.programmes.find(x => x.id === id);
  SLOTS.forEach(slot => {
    if (p.slots[slot.code] === 'ORDINAIRE') return;
    const el = document.getElementById('pd-slot-' + slot.code);
    if (el) p.slots[slot.code] = el.value || null;
  });
  persist(); toast('Programme mis à jour'); render();
}
function isChantComplete(chant) {
  return !!chant && chantIsDocComplete(chant);
}
function programmeIncompleteChants(p) {
  const out = [];
  SLOTS.forEach(slot => {
    const val = p.slots[slot.code];
    if (!val || val === 'MENTION' || val === 'ORDINAIRE') return;
    const chant = DB.chants.find(c => c.id == val);
    if (!chant) { out.push({ slot: slot.label, titre: '(chant supprimé)', reason: 'chant introuvable' }); return; }
    if (!isChantComplete(chant)) {
      const reasons = [];
      if (!chantHasMedia(chant)) reasons.push('partition, audio ou vidéo manquant');
      if (!chant.texte) reasons.push('texte manquant');
      out.push({ slot: slot.label, titre: chant.titre, reason: reasons.join(', ') });
    }
  });
  return out;
}
function validateProgramme(id) {
  const p = DB.programmes.find(x => x.id === id);
  const incomplete = programmeIncompleteChants(p);
  if (incomplete.length) {
    toast(`Validation impossible : ${incomplete.length} chant(s) incomplet(s) (partition/audio manquant)`);
    return;
  }
  p.statut = 'validé'; persist(); toast('Programme validé'); nav('#/programmes'); render();
}
function deleteProgramme(id) {
  if (!confirm('Supprimer ce programme ?')) return;
  DB.programmes = DB.programmes.filter(p => p.id !== id);
  persist(); toast('Programme supprimé'); nav('#/programmes'); render();
}
function sendNotification(progId) {
  const p = DB.programmes.find(x => x.id === progId);
  const totalUsers = DB.animateurs.length + DB.musiciens.length;
  DB.notifications.push({ date: new Date().toISOString(), programme: p.titre, count: totalUsers });
  persist();
  toast(`Notification envoyée à ${totalUsers} utilisateurs`);
}
/* ---------------- Calendrier ---------------- */
function musicienLabel(id) {
  const m = DB.musiciens.find(x => x.id == id);
  return m ? `${m.nom} ${m.prenom}` : '—';
}
function animateurLabel(id) {
  const a = DB.animateurs.find(x => x.id == id);
  return a ? `${a.nom} ${a.prenom}` : '—';
}
function findProgrammeForOffice(o) {
  if (o.programmeId) {
    const linked = DB.programmes.find(p => p.id === o.programmeId);
    if (linked) return linked;
  }
  return DB.programmes.find(p => p.date === o.date && String(p.lieu||'') === String(o.lieu||''));
}
function programmesForOfficeDropdown(o) {
  const sameDateLieu = DB.programmes.filter(p => p.date === o.date && String(p.lieu||'') === String(o.lieu||''));
  const sameDateOnly = DB.programmes.filter(p => p.date === o.date && String(p.lieu||'') !== String(o.lieu||''));
  return { sameDateLieu, sameDateOnly };
}
function openLinkProgrammeSelector(officeId) {
  window._linkingOffice = officeId;
  render();
}
function linkProgrammeToOffice(officeId, programmeId) {
  const o = DB.calendrier.find(x => x.id === officeId);
  o.programmeId = programmeId ? Number(programmeId) : null;
  window._linkingOffice = null;
  persist(); toast('Programme relié'); render();
}
function pageDatesVacantes() {
  if (!can('edit')) return pageForbidden();
  const weeksAhead = window._vacantWeeks || 8;
  const vacant = computeVacantDates(weeksAhead);
  const jours = ['Dimanche','Lundi','Mardi','Mercredi','Jeudi','Vendredi','Samedi'];
  const marked = [...DB.noOffice].sort((a,b) => a.date.localeCompare(b.date));
  return `
  <div class="page-head">
    <div>
      <div class="crumb">Préparation</div>
      <h1>Dates vacantes</h1>
      <p class="muted">Samedis et dimanches des ${weeksAhead} prochaines semaines sans office créé, ou avec un office incomplet (sans organiste ou sans chantre), par clocher.</p>
    </div>
    <select onchange="window._vacantWeeks=Number(this.value);render()">
      <option value="4" ${weeksAhead===4?'selected':''}>4 semaines</option>
      <option value="8" ${weeksAhead===8?'selected':''}>8 semaines</option>
      <option value="12" ${weeksAhead===12?'selected':''}>12 semaines</option>
    </select>
  </div>
  <div class="card" style="margin-bottom:16px;">
    ${vacant.length ? `<div class="chant-list">${vacant.map(v => `
      <div class="chant-row" style="cursor:default;">
        <div class="rib" style="background:${v.incomplete ? 'var(--gold)' : 'var(--wine)'}"></div>
        <div style="flex:1;">
          <div class="chant-title" style="font-size:15px;">${jours[parseLocalDate(v.date).getDay()]} ${fmtDate(v.date)}</div>
          <div class="chant-meta">${clocherLabel(v.lieu)} ${v.incomplete ? '· <span class="badge gold" style="margin-left:2px;">office incomplet</span>' : ''}</div>
        </div>
        ${v.incomplete
          ? `<button onclick="goToEditOffice(${v.officeId})">Compléter l’office</button>`
          : `<button onclick="goToVacantOffice('${v.date}','${v.lieu}')">+ Ajouter un office</button>`}
        <button class="ghost" onclick="markNoOffice('${v.date}','${v.lieu}')">Pas d’office</button>
      </div>`).join('')}</div>` : `<div class="empty"><h3>Aucune date vacante</h3><p class="muted">Tous les samedis/dimanches à venir ont un office complet ou sont marqués « pas d’office ».</p></div>`}
  </div>
  ${marked.length ? `<div class="card">
    <h3>Dates marquées « pas d’office »</h3>
    <div class="chant-list">${marked.map(m => `
      <div class="chant-row" style="cursor:default;">
        <div class="rib" style="background:var(--sage)"></div>
        <div style="flex:1;">
          <div class="chant-title" style="font-size:14px;">${jours[parseLocalDate(m.date).getDay()]} ${fmtDate(m.date)}</div>
          <div class="chant-meta">${clocherLabel(m.lieu)}</div>
        </div>
        <button class="ghost" onclick="unmarkNoOffice('${m.date}','${m.lieu}')">Annuler</button>
      </div>`).join('')}</div>
  </div>` : ''}
  `;
}
function pageCalendrier() {
  const list = [...DB.calendrier].sort((a,b)=>a.date.localeCompare(b.date));
  if (window._prefillCalDate) {
    const pd = window._prefillCalDate, pl = window._prefillCalLieu;
    window._prefillCalDate = null; window._prefillCalLieu = null;
    setTimeout(() => openCalForm(null, pd, pl), 0);
  }
  if (window._editCalId) {
    const eid = window._editCalId;
    window._editCalId = null;
    setTimeout(() => openCalForm(eid), 0);
  }
  return `
  <div class="page-head">
    <div>
      <div class="crumb">Préparation</div>
      <h1>Calendrier d’animation liturgique</h1>
      <p class="muted">Équipes, horaires et lieux pour une période donnée (2 mois maximum recommandé).</p>
    </div>
    ${can('edit') ? `<button class="primary" onclick="openCalForm()">+ Ajouter un office</button>` : ''}
  </div>
  <div id="cal-form-zone"></div>
  <div class="card">
    <table>
      <thead><tr><th>Date</th><th>Horaire</th><th>Lieu</th><th>Événement</th><th>Organiste</th><th>Chantre</th>${(DB.schema.calendrier||[]).map(f=>`<th>${esc(f.label)}</th>`).join('')}<th>Programme</th>${can('edit')?'<th></th>':''}</tr></thead>
      <tbody>
      ${list.length ? list.map(o => {
        const prog = findProgrammeForOffice(o);
        const rowClick = can('edit') ? `onclick="openCalForm(${o.id})" style="cursor:pointer;"` : '';
        let progCell;
        if (window._linkingOffice === o.id && can('edit')) {
          const { sameDateLieu, sameDateOnly } = programmesForOfficeDropdown(o);
          progCell = `<div onclick="event.stopPropagation()">
            <select onchange="linkProgrammeToOffice(${o.id}, this.value)">
              <option value="">+ Créer un nouveau programme</option>
              ${sameDateLieu.map(p => `<option value="${p.id}" ${prog&&prog.id===p.id?'selected':''}>${esc(p.titre)} (même date/lieu)</option>`).join('')}
              ${sameDateOnly.map(p => `<option value="${p.id}" ${prog&&prog.id===p.id?'selected':''}>${esc(p.titre)} — ${clocherLabel(p.lieu)} (lieu différent)</option>`).join('')}
            </select>
          </div>`;
        } else if (prog) {
          progCell = `<a onclick="event.stopPropagation();nav('#/programmes/${prog.id}')" style="cursor:pointer;"><span class="badge ${prog.statut==='validé'?'sage':'gray'}">${prog.statut}</span></a>
            ${can('edit') ? `<a onclick="event.stopPropagation();openLinkProgrammeSelector(${o.id})" style="cursor:pointer;font-size:11px;margin-left:6px;">changer</a>` : ''}`;
        } else {
          const { sameDateLieu, sameDateOnly } = programmesForOfficeDropdown(o);
          if (can('edit') && (sameDateLieu.length || sameDateOnly.length)) {
            progCell = `<a onclick="event.stopPropagation();openLinkProgrammeSelector(${o.id})" style="cursor:pointer;"><span class="badge gray">relier un programme</span></a>`;
          } else {
            progCell = can('edit')
              ? `<a onclick="event.stopPropagation();goToOfficeProgramme('${o.date}','${o.lieu||''}')" style="cursor:pointer;"><span class="badge gray">créer</span></a>`
              : `<span class="muted">aucun programme</span>`;
          }
        }
        return `
        <tr ${rowClick}>
          <td>${fmtDate(o.date)}</td>
          <td>${esc(o.horaire)||'—'}</td>
          <td>${clocherLabel(o.lieu)}</td>
          <td>${o.evenement ? typeMesseLabel(o.evenement) : 'Temps ordinaire'}</td>
          <td>${o.organiste ? musicienLabel(o.organiste) : '—'}</td>
          <td>${o.chantre ? animateurLabel(o.chantre) : '—'}</td>
          ${(DB.schema.calendrier||[]).map(f=>`<td>${esc(o.custom && o.custom[f.key]) || '—'}</td>`).join('')}
          <td>${progCell}</td>
          ${can('edit') ? `<td class="no-print"><button class="ghost" onclick="event.stopPropagation();deleteCal(${o.id})">Supprimer</button></td>` : ''}
        </tr>`;
      }).join('') : `<tr><td colspan="${8 + (DB.schema.calendrier||[]).length}" class="muted" style="text-align:center;padding:24px;">Aucun office renseigné.</td></tr>`}
      </tbody>
    </table>
  </div>`;
}
function resolveClocherIdByLabel(text) {
  text = (text || '').trim();
  if (!text) return null;
  const found = Object.entries(DB.clochers).find(([k,v]) => v.toLowerCase() === text.toLowerCase());
  return found ? found[0] : null;
}
function getOrCreateClocherId(text) {
  const existing = resolveClocherIdByLabel(text);
  if (existing) return existing;
  const label = text.trim();
  if (!label) return null;
  const nextId = Object.keys(DB.clochers).reduce((m,k) => Math.max(m, Number(k)||0), 0) + 1;
  DB.clochers[nextId] = label;
  return String(nextId);
}
function chantreOptions(lieuId) {
  const list = DB.animateurs.filter(a => String(a.clocher) === String(lieuId));
  if (!list.length) return `<option value="">— aucun animateur sur ce clocher —</option>`;
  return `<option value="">—</option>` + list.map(a => `<option value="${a.id}">${esc(a.nom)} ${esc(a.prenom)}</option>`).join('');
}
function updateChantreOptions() {
  const text = document.getElementById('cf-lieu').value;
  const lieu = resolveClocherIdByLabel(text);
  document.getElementById('cf-chantre').innerHTML = lieu ? chantreOptions(lieu) : `<option value="">— nouveau clocher : aucun animateur encore assigné —</option>`;
}
function openCalForm(editId, prefillDate, prefillLieu) {
  const o = editId ? DB.calendrier.find(x => x.id === editId) : null;
  const firstLieu = o ? o.lieu : (prefillLieu || Object.keys(DB.clochers)[0]);
  document.getElementById('cal-form-zone').innerHTML = `
  <div class="card" style="margin-bottom:16px;max-width:760px;">
    <h3>${o ? 'Modifier l’office' : 'Nouvel office'}</h3>
    <div class="grid3">
      <div class="field"><label>Date *</label><input type="date" id="cf-date" value="${o ? o.date : (prefillDate || todayISO())}"></div>
      <div class="field"><label>Horaire</label><input id="cf-horaire" placeholder="10:30" value="${esc(o ? o.horaire : '')}"></div>
      <div class="field"><label>Lieu (clocher) <span class="muted">— choisissez ou saisissez un nouveau nom</span></label>
        <input id="cf-lieu" list="dl-clochers" value="${esc(clocherLabel(firstLieu))}" oninput="updateChantreOptions()">
        <datalist id="dl-clochers">${Object.values(DB.clochers).map(v=>`<option value="${esc(v)}">`).join('')}</datalist>
      </div>
      <div class="field"><label>Événement</label>
        <select id="cf-evenement"><option value="">Temps ordinaire</option>${Object.entries(DB.type_messe).map(([k,v])=>`<option value="${k}" ${o && o.evenement===k?'selected':''}>${v}</option>`).join('')}</select>
      </div>
      <div class="field"><label>Organiste</label>
        <select id="cf-organiste"><option value="">—</option>${DB.musiciens.map(m=>`<option value="${m.id}" ${o && String(o.organiste)===String(m.id)?'selected':''}>${esc(m.nom)} ${esc(m.prenom)}</option>`).join('')}</select>
      </div>
      <div class="field"><label>Chantre <span class="muted">(du clocher sélectionné)</span></label>
        <select id="cf-chantre">${chantreOptions(firstLieu)}</select>
      </div>
      ${renderCustomFieldInputs('calendrier', o || {})}
    </div>
    <div class="row">
      <button class="primary" onclick="saveCal(${editId || 'null'})">Enregistrer</button>
      <button class="ghost" onclick="document.getElementById('cal-form-zone').innerHTML=''">Annuler</button>
    </div>
  </div>`;
  if (o && o.chantre) document.getElementById('cf-chantre').value = o.chantre;
}
function saveCal(editId) {
  const val = i => document.getElementById(i).value.trim();
  const date = val('cf-date');
  if (!date) { toast('La date est obligatoire'); return; }
  const lieu = getOrCreateClocherId(val('cf-lieu'));
  if (!lieu) { toast('Le lieu est obligatoire'); return; }
  const data = { date, horaire: val('cf-horaire'), lieu, evenement: val('cf-evenement')||null,
    organiste: val('cf-organiste')||null, chantre: val('cf-chantre')||null, custom: readCustomFieldValues('calendrier') };
  if (editId) {
    const idx = DB.calendrier.findIndex(o => o.id === editId);
    DB.calendrier[idx] = { ...DB.calendrier[idx], ...data };
    toast('Office mis à jour');
  } else {
    DB.calendrier.push({ id: (DB.calendrier.reduce((m,o)=>Math.max(m,o.id||0),0))+1, ...data });
    toast('Office ajouté');
  }
  persist(); document.getElementById('cal-form-zone').innerHTML=''; render();
}
function deleteCal(id) {
  DB.calendrier = DB.calendrier.filter(o => o.id !== id);
  persist(); toast('Office supprimé'); render();
}

/* ---------------- Animateurs / Musiciens (CRUD) ---------------- */
function pagePersonnes(kind) {
  const isAnim = kind === 'animateurs';
  const title = isAnim ? 'Animateurs' : 'Musiciens';
  const sf = window._personSearch && window._personSearch.kind === kind ? window._personSearch : { nom:'', prenom:'', instrument:'', clocher:'' };
  const list = filterPersonnes(kind, sf);
  const all = isAnim ? DB.animateurs : DB.musiciens;
  return `
  <div class="page-head">
    <div>
      <div class="crumb">Équipes</div>
      <h1>${title}</h1>
      <p class="muted">${all.length} personne(s) réparties sur ${Object.keys(DB.clochers).length} clochers.</p>
    </div>
    ${can('manage-personnes') ? `<button class="primary" onclick="openPersonForm('${kind}')">+ Ajouter</button>` : ''}
  </div>
  <div class="card" style="margin-bottom:16px;">
    <h3>Rechercher un ${isAnim ? 'animateur' : 'musicien'}</h3>
    <div class="grid3">
      <div class="field"><label>Nom</label><input id="ps-nom" value="${esc(sf.nom)}"></div>
      <div class="field"><label>Prénom</label><input id="ps-prenom" value="${esc(sf.prenom)}"></div>
      ${isAnim ? '' : `<div class="field"><label>Instrument</label>
        <select id="ps-instrument"><option value="">—</option>${Object.entries(DB.instruments).map(([k,v])=>`<option value="${k}" ${sf.instrument===k?'selected':''}>${v}</option>`).join('')}</select>
      </div>`}
      <div class="field"><label>Clocher</label>
        <select id="ps-clocher"><option value="">—</option>${Object.entries(DB.clochers).map(([k,v])=>`<option value="${k}" ${String(sf.clocher)===String(k)?'selected':''}>${v}</option>`).join('')}</select>
      </div>
    </div>
    <div class="row">
      <button class="primary" onclick="runSearchPerson('${kind}')">Rechercher</button>
      ${(sf.nom||sf.prenom||sf.instrument||sf.clocher) ? `<button class="ghost" onclick="clearSearchPerson('${kind}')">Réinitialiser</button>` : ''}
    </div>
  </div>
  <div id="person-form-zone"></div>
  <div class="card">
    <table>
      <thead><tr><th>Nom</th><th>Prénom</th>${isAnim?'':'<th>Instrument</th>'}<th>Clocher</th><th>Arrivée</th>${(DB.schema[kind]||[]).map(f=>`<th>${esc(f.label)}</th>`).join('')}${can('manage-personnes')?'<th></th>':''}</tr></thead>
      <tbody>
      ${list.length ? list.map(p => `
        <tr>
          <td>${esc(p.nom)}</td><td>${esc(p.prenom)}</td>
          ${isAnim ? '' : `<td>${instrumentLabel(p.instrument)}</td>`}
          <td>${p.clocher ? clocherLabel(p.clocher) : '—'}</td>
          <td>${p.date_arrivee ? fmtDate(p.date_arrivee) : '—'}</td>
          ${(DB.schema[kind]||[]).map(f=>`<td>${esc(p.custom && p.custom[f.key]) || '—'}</td>`).join('')}
          ${can('manage-personnes') ? `<td class="no-print"><button class="ghost" onclick="openPersonForm('${kind}',${p.id})">Modifier</button> <button class="ghost" onclick="deletePerson('${kind}',${p.id})">Supprimer</button></td>` : ''}
        </tr>`).join('') : `<tr><td colspan="6" class="muted" style="text-align:center;padding:20px;">Aucun résultat pour cette recherche.</td></tr>`}
      </tbody>
    </table>
  </div>`;
}
function filterPersonnes(kind, sf) {
  const isAnim = kind === 'animateurs';
  const list = isAnim ? DB.animateurs : DB.musiciens;
  if (!sf.nom && !sf.prenom && !sf.instrument && !sf.clocher) return list;
  return list.filter(p => {
    if (sf.nom && !(p.nom||'').toLowerCase().includes(sf.nom.toLowerCase())) return false;
    if (sf.prenom && !(p.prenom||'').toLowerCase().includes(sf.prenom.toLowerCase())) return false;
    if (!isAnim && sf.instrument && p.instrument !== sf.instrument) return false;
    if (sf.clocher && String(p.clocher) !== String(sf.clocher)) return false;
    return true;
  });
}
function runSearchPerson(kind) {
  const isAnim = kind === 'animateurs';
  const nom = document.getElementById('ps-nom').value.trim();
  const prenom = document.getElementById('ps-prenom').value.trim();
  const instrument = isAnim ? '' : document.getElementById('ps-instrument').value;
  const clocher = document.getElementById('ps-clocher').value;
  if (!nom && !prenom && !instrument && !clocher) { toast('Renseignez au moins un critère de recherche'); return; }
  window._personSearch = { kind, nom, prenom, instrument, clocher };
  render();
}
function clearSearchPerson(kind) {
  window._personSearch = null;
  render();
}
function openPersonForm(kind, editId) {
  if (!can('manage-personnes')) return;
  const isAnim = kind === 'animateurs';
  const list = isAnim ? DB.animateurs : DB.musiciens;
  const p = editId ? list.find(x => x.id === editId) : null;
  document.getElementById('person-form-zone').innerHTML = `
  <div class="card" style="margin-bottom:16px;max-width:640px;">
    <h3>${p ? 'Modifier' : 'Ajouter'} ${isAnim ? 'un animateur' : 'un musicien'}</h3>
    <div class="grid2">
      <div class="field"><label>Nom *</label><input id="pf2-nom" value="${esc(p ? p.nom : '')}"></div>
      <div class="field"><label>Prénom *</label><input id="pf2-prenom" value="${esc(p ? p.prenom : '')}"></div>
      ${isAnim ? '' : `<div class="field"><label>Instrument</label><select id="pf2-instrument">${Object.entries(DB.instruments).map(([k,v])=>`<option value="${k}" ${p&&p.instrument===k?'selected':''}>${v}</option>`).join('')}</select></div>`}
      <div class="field"><label>Clocher</label><select id="pf2-clocher">${Object.entries(DB.clochers).map(([k,v])=>`<option value="${k}" ${p&&String(p.clocher)===String(k)?'selected':''}>${v}</option>`).join('')}</select></div>
      <div class="field"><label>Date d’arrivée</label><input type="date" id="pf2-date" value="${p&&p.date_arrivee ? p.date_arrivee : ''}"></div>
      ${renderCustomFieldInputs(kind, p || {})}
    </div>
    <div class="row">
      <button class="primary" onclick="savePerson('${kind}'${editId ? ','+editId : ''})">Enregistrer</button>
      <button class="ghost" onclick="document.getElementById('person-form-zone').innerHTML=''">Annuler</button>
    </div>
  </div>`;
}
function savePerson(kind, editId) {
  if (!can('manage-personnes')) return;
  const isAnim = kind === 'animateurs';
  const val = i => document.getElementById(i).value.trim();
  const nom = val('pf2-nom'), prenom = val('pf2-prenom');
  if (!nom || !prenom) { toast('Nom et prénom obligatoires'); return; }
  const data = { nom, prenom, date_arrivee: val('pf2-date')||null, clocher: val('pf2-clocher')?Number(val('pf2-clocher')):null, custom: readCustomFieldValues(kind) };
  if (!isAnim) data.instrument = val('pf2-instrument');
  const list = isAnim ? DB.animateurs : DB.musiciens;
  if (editId) {
    const idx = list.findIndex(p => p.id === editId);
    list[idx] = { ...list[idx], ...data };
    toast('Modifié');
  } else {
    const idKey = isAnim ? 'nextAnimateurId' : 'nextMusicienId';
    list.push({ id: DB[idKey]++, ...data });
    toast('Enregistré');
  }
  persist(); document.getElementById('person-form-zone').innerHTML=''; render();
}
function deletePerson(kind, id) {
  if (!can('manage-personnes')) return;
  if (!confirm('Confirmer la suppression ?')) return;
  if (kind === 'animateurs') DB.animateurs = DB.animateurs.filter(p => p.id !== id);
  else DB.musiciens = DB.musiciens.filter(p => p.id !== id);
  persist(); toast('Supprimé'); render();
}

/* ---------------- Admin (WebMaster) ---------------- */
function pageAdmin() {
  const activeTable = window._adminTable || 'type_messe';
  const TABLES = {
    type_messe: 'Catégories (type de messe)', type_chant: 'Types de chant',
    ordinaire_messe: 'Ordinaires de messe', instruments: 'Instruments', clochers: 'Clochers (paroisse)',
  };
  return `
  <div class="page-head">
    <div>
      <div class="crumb">Administration</div>
      <h1>WebMaster</h1>
      <p class="muted">Maintenance, évolution et sécurité de l’outil.</p>
    </div>
  </div>
  <div class="grid2" style="margin-bottom:16px;">
    <div class="card">
      <h3>Notifications envoyées</h3>
      ${DB.notifications.length ? `<table><thead><tr><th>Date</th><th>Programme</th><th>Destinataires</th></tr></thead><tbody>
        ${[...DB.notifications].reverse().map(n => `<tr><td>${new Date(n.date).toLocaleString('fr-FR')}</td><td>${esc(n.programme)}</td><td>${n.count}</td></tr>`).join('')}
      </tbody></table>` : `<p class="muted">Aucune notification envoyée pour le moment.</p>`}
    </div>
    <div class="card">
      <h3>Données de l’application</h3>
      <p class="muted">Toutes les données sont stockées localement dans ce navigateur.</p>
      <div class="row" style="margin-top:10px;flex-wrap:wrap;">
        <button onclick="exportData()">Exporter (JSON)</button>
        <button onclick="document.getElementById('import-file').click()">Réimporter un JSON</button>
        <input type="file" id="import-file" accept="application/json" style="display:none" onchange="importData(this.files[0])">
        <button class="danger" onclick="resetData()">Réinitialiser les données</button>
      </div>
    </div>
  </div>
  <div class="card" style="margin-bottom:16px;">
    <h3>Tables de référence</h3>
    <p class="muted" style="margin-top:-6px;">Une IHM par table d’entité, comme demandé au cahier des charges.</p>
    <div class="toolbar">
      ${Object.entries(TABLES).map(([k,label]) => `<button class="${activeTable===k?'primary':''}" onclick="window._adminTable='${k}';render()">${label}</button>`).join('')}
    </div>
    ${renderRefTableEditor(activeTable)}
  </div>
  <div class="card">
    <h3>Structure de la base de données</h3>
    <p class="muted" style="margin-top:-6px;">Réservé au WebMaster. Consultez les champs existants de chaque table, et ajoutez des champs personnalisés si besoin — ils apparaîtront ensuite dans les formulaires correspondants.</p>
    <div class="toolbar">
      ${Object.entries(STRUCT_TABLES).map(([k,label]) => `<button class="${(window._structTable||'chants')===k?'primary':''}" onclick="window._structTable='${k}';render()">${label}</button>`).join('')}
    </div>
    ${renderStructureEditor(window._structTable || 'chants')}
  </div>`;
}
const STRUCT_TABLES = { chants: 'Chants', musiciens: 'Musiciens', animateurs: 'Animateurs', calendrier: 'Calendrier' };
const STRUCT_BUILTIN_FIELDS = {
  chants: [['titre','Texte'],['reference1','Texte'],['reference2','Texte'],['texte','Texte long'],['type_messe','Référence'],['type_chant','Référence'],['compositeur','Texte'],['harmonisation','Texte'],['partition','Texte/URL'],['youtube','URL'],['audios','Liste de pistes']],
  musiciens: [['nom','Texte'],['prenom','Texte'],['instrument','Référence'],['clocher','Référence'],['date_arrivee','Date']],
  animateurs: [['nom','Texte'],['prenom','Texte'],['clocher','Référence'],['date_arrivee','Date']],
  calendrier: [['date','Date'],['horaire','Texte'],['lieu','Référence'],['evenement','Référence'],['organiste','Référence musicien'],['chantre','Référence animateur']],
};
function renderStructureEditor(entity) {
  const builtin = STRUCT_BUILTIN_FIELDS[entity] || [];
  const custom = (DB.schema && DB.schema[entity]) || [];
  return `
  <h4 style="font-size:13px;margin:4px 0 6px;">Champs existants</h4>
  <table style="margin-bottom:14px;">
    <thead><tr><th>Champ</th><th>Libellé</th><th>Type</th></tr></thead>
    <tbody>
      ${builtin.map(([k,t]) => `<tr><td style="font-family:monospace;">${esc(k)}</td><td class="muted">—</td><td class="muted">${esc(t)} <span class="muted" style="font-size:10.5px;">(fixe)</span></td></tr>`).join('')}
      ${custom.map(f => `<tr>
        <td style="font-family:monospace;">${esc(f.key)} <span class="badge gold" style="margin-left:4px;">personnalisé</span></td>
        <td><input value="${esc(f.label)}" onchange="updateStructField('${entity}','${f.key}','label',this.value)"></td>
        <td><select onchange="updateStructField('${entity}','${f.key}','type',this.value)">
          <option value="text" ${f.type==='text'?'selected':''}>Texte</option>
          <option value="number" ${f.type==='number'?'selected':''}>Nombre</option>
          <option value="date" ${f.type==='date'?'selected':''}>Date</option>
        </select></td>
      </tr>`).join('')}
    </tbody>
  </table>
  <p class="muted" style="margin-top:-8px;font-size:12px;">Les champs natifs (fixes) ne sont pas modifiables : leur type est lié au fonctionnement de l’application. Seuls les champs personnalisés que vous ajoutez peuvent être renommés ou retypés ici.</p>
  <h4 style="font-size:13px;margin:4px 0 6px;">Ajouter un champ personnalisé</h4>
  <div class="grid3" style="align-items:end;">
    <div class="field"><label>Clé (identifiant)</label><input id="struct-new-key" placeholder="ex. tonalite" style="font-family:monospace;"></div>
    <div class="field"><label>Libellé affiché</label><input id="struct-new-label" placeholder="ex. Tonalité"></div>
    <div class="field"><label>Type</label>
      <select id="struct-new-type"><option value="text">Texte</option><option value="number">Nombre</option><option value="date">Date</option></select>
    </div>
  </div>
  <button class="primary" onclick="addStructField('${entity}')">Ajouter le champ</button>
  ${custom.length ? `
  <h4 style="font-size:13px;margin:16px 0 6px;">Supprimer un champ personnalisé</h4>
  <div class="row" style="flex-wrap:wrap;gap:8px;">
    ${custom.map(f => `<button class="ghost" onclick="removeStructField('${entity}','${f.key}')">✕ ${esc(f.label)}</button>`).join('')}
  </div>` : ''}
  `;
}
function updateStructField(entity, key, prop, value) {
  const field = (DB.schema[entity] || []).find(f => f.key === key);
  if (!field) return;
  field[prop] = value;
  persist(); toast('Champ mis à jour');
}
function addStructField(entity) {
  const key = document.getElementById('struct-new-key').value.trim().toLowerCase().replace(/[^a-z0-9_]/g,'');
  const label = document.getElementById('struct-new-label').value.trim();
  const type = document.getElementById('struct-new-type').value;
  if (!key || !label) { toast('Clé et libellé requis'); return; }
  DB.schema[entity] = DB.schema[entity] || [];
  if (DB.schema[entity].find(f => f.key === key)) { toast('Cette clé existe déjà'); return; }
  DB.schema[entity].push({ key, label, type });
  persist(); toast('Champ personnalisé ajouté'); render();
}
function removeStructField(entity, key) {
  if (!confirm('Supprimer ce champ personnalisé ? Les valeurs déjà saisies seront perdues.')) return;
  DB.schema[entity] = (DB.schema[entity]||[]).filter(f => f.key !== key);
  persist(); toast('Champ supprimé'); render();
}
function renderRefTableEditor(key) {
  const dict = DB[key] || {};
  const entries = Object.entries(dict);
  return `
  <table>
    <thead><tr><th>Code</th><th>Libellé</th><th></th></tr></thead>
    <tbody>
      ${entries.map(([code, label]) => `
        <tr>
          <td style="font-family:monospace;">${esc(code)}</td>
          <td><input value="${esc(label)}" onchange="updateRefEntry('${key}','${code}',this.value)"></td>
          <td class="no-print"><button class="ghost" onclick="deleteRefEntry('${key}','${code}')">Supprimer</button></td>
        </tr>`).join('')}
      <tr>
        <td><input id="ref-new-code" placeholder="Code" style="font-family:monospace;"></td>
        <td><input id="ref-new-label" placeholder="Libellé"></td>
        <td class="no-print"><button class="primary" onclick="addRefEntry('${key}')">Ajouter</button></td>
      </tr>
    </tbody>
  </table>`;
}
function updateRefEntry(key, code, value) {
  DB[key][code] = value; persist(); toast('Mis à jour');
}
function deleteRefEntry(key, code) {
  if (!confirm('Supprimer cette entrée de la table ?')) return;
  delete DB[key][code]; persist(); render();
}
function addRefEntry(key) {
  const code = document.getElementById('ref-new-code').value.trim();
  const label = document.getElementById('ref-new-label').value.trim();
  if (!code || !label) { toast('Code et libellé requis'); return; }
  DB[key][code] = label; persist(); toast('Entrée ajoutée'); render();
}
function exportData() {
  const blob = new Blob([JSON.stringify(DB, null, 2)], { type: 'application/json' });
  const a = document.createElement('a');
  a.href = URL.createObjectURL(blob);
  a.download = 'gestoffice-export.json';
  a.click();
}
function importData(file) {
  if (!file) return;
  const reader = new FileReader();
  reader.onload = () => {
    try {
      const data = JSON.parse(reader.result);
      if (!data || typeof data !== 'object' || !Array.isArray(data.chants)) throw new Error('format invalide');
      DB = data; persist(); toast('Base de données réimportée'); nav('#/accueil'); render();
    } catch (e) {
      toast('Fichier JSON invalide');
    }
  };
  reader.readAsText(file);
}
function resetData() {
  if (!confirm('Réinitialiser toutes les données à l’état initial ? Cette action est irréversible.')) return;
  localStorage.removeItem(LS_KEY);
  DB = loadDB();
  toast('Données réinitialisées'); nav('#/accueil'); render();
}
